# Project TODO List

Still-open follow-up items. Adapted from `fayf_smartskills/.ai/todo.md`'s
extended format.

**Schema:** `ID | Title | Priority | Status | Comment | Dependencies | Created | Updated`
- **Priority:** `Critical` / `High` / `Medium` / `Low`.
- **Status:** `open` / `in-progress` / `blocked` / `done` / `dropped`.
- **Comment:** why the item exists, current thinking.
- **Dependencies:** comma-separated IDs this item can't usefully start before;
  empty when there's no real gating relationship.
- **Created / Updated:** `YYYY-MM-DD`.

Closing an item sets `Status: done`, `Updated`, then moves the row to
`.ai/archive.md` (newest-first) in the same change.

| ID | Title | Priority | Status | Comment | Dependencies | Created | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-1 | Author line-art icons for Shop/Training/Lecture/Free Learning | Medium | open | Only `icon-dashboard` ported so far (Heroicons v2 outline, MIT, via `infopedia_processor/frontend/icon-sprite.js`). Remaining 4 nav ids fall back to `ui/icons.js`'s shared `icon-placeholder` glyph (dashed-circle outline). Hand-author own line-art per that repo's own precedent (its `D-R4-INLINE` category icons), add to `ui/icons.js`'s `ICONS` list + sprite -- each replaces the placeholder glyph automatically once listed. | | 2026-08-28 | 2026-08-28 |
| TODO-2 | Wire real pointer-proximity rollout for the nav rail (closed → expanded on approach) | Medium | open | `component/app-nav-rail` (closed, icon-only) and `component/app-nav-rail.expanded` (icon+text) exist as static variants. The dynamic "roll out on approach" behavior needs a new L3 relation measure in `ui/relation.js` (today only `scroll-fraction` exists) — analogous to `component/scrollbar`'s existing `relation` usage, so the variant would still only declare it via JSON once the measure exists. | | 2026-08-28 | 2026-08-28 |
| TODO-3 | Build remaining workspace screens: Shop, Training, Lecture, Free Learning | Medium | open | Step1 shipped only `screens/math-trainer-dashboard.json`. Nav rail already scaffolds all 5 sections (4 disabled). Each new screen composes from existing L0-L7 parts, same pattern as the dashboard. | | 2026-08-28 | 2026-08-28 |
| TODO-6 | Interaction state rules — pressed/loading/error/read-only added to the actionable-box layer | Medium | in-progress | Imported from Claude Design handoff "Element state style rules" (`State Rules Wireframes.dc.html`, project `2baedd3e-1686-4433-8561-fcc1b58a9eaf`) — verbatim copy `docs/state-rules.reference.html`. Doc offered 4 alternative visual languages for encoding interaction state across button/link/checkbox/toggle/nav-item roles (A: opacity ladder, B: 9 named colours, C: monochrome weight/elevation, D: hybrid one-channel-per-state); user picked **D** per C9 one-at-a-time ask, since it's closest to what fayf_ui already shipped (hover=brightness filter, focus=dashed outline ring, engaged/selected=solid outline — each already its own channel). Spec: `docs/superpowers/specs/2026-08-30-state-rules-design.md`. Scoped down from the doc's 6 component roles to fayf_ui's one existing role-agnostic mechanism (`ui/actions.js`'s "actionable box") — inventing button/checkbox/toggle/nav-item as real parts is separate, unscoped work. Shipped: `ui/tokens.css` gained `.bx-loading`(opacity:.7, inert), `.bx-error`(border-color:accent, stays actionable), `.bx-readonly`(opacity:.55, inert), and `.bx-actionable:active` (pressed, inset box-shadow); `ui/actions.js` gained `setActionableLoading`/`setActionableError`/`setActionableReadonly` mirroring the existing `setActionableDisabled` pairing with tab order (error does NOT drop tab order — doc's own "Retry stays actionable" rule). New fixture `state-rules.html` + `test/state-rules_test.js` (self-contained, same role as `inspector.html`/`space-class.html`) — all green, plus manual real-Tab-key check in the Browser pane (screenshot + `getComputedStyle`) confirming states render identically in both wireframe and mockup skins (corrected an initial wrong assumption in the spec draft that these needed a mockup-only gate — checked against `tokens.css` first, existing hover/focus/disabled/selected already skin-agnostic). **Real bug found + fixed during testing**: `.bx-error{outline:...}` (first draft) silently lost to `:focus-visible`'s own `outline` rule on a focused+errored box — `outline` is non-stacking and focus-visible has higher specificity. Moved error to `border-color` (a channel nothing else claims) + a specificity-matched mockup-skin duplicate rule; added a regression test that introspects the stylesheet rules directly (`declaredProps()` in `test/state-rules_test.js`) rather than relying on triggering `:focus-visible` — confirmed via `node -e`-style probing that headless `.focus()`/`{focusVisible:true}` don't reproduce the browser's real focus-visible heuristic in this environment, so that specific proof stayed manual (Tab key, Browser pane), consistent with this repo's existing precedent for interaction-driven behavior untestable headlessly (see TODO-5's space-class `ResizeObserver` note). **Logged, not fixed**: `.bx-selected` (engaged) has the identical outline-collision with focus — predates this doc, is shipped/tested quiz behavior, out of scope to retrofit today; a simultaneously focused+engaged box shows one ring or the other, not both. **Also found, spun off separately** (not part of this entry): `test/node/registry.js`'s `loadRegistry()` uses `new URL(...).pathname`, which returns a Windows-broken leading-slash drive path (`/D:/...`) — `globSync` silently returns 0 files, so `just ci` fails 2 of its files (`parts_validate_test.js`, `answer_variant_test.js`) on Windows with no real regression. Verified the other 5 node test files unaffected (66/66 pass, they carry their own fixture registries). **Fixed 2026-09-09** (TODO-7 round 2): `fileURLToPath` + separator normalisation, node 76/76 on Windows. Not yet done: wiring any of the 4 new states into a real screen's content (explicit non-goal in the spec — which screen/component needs pressed/loading/error/read-only first is an open follow-up), and the component-role split (primary/secondary/tertiary button, checkbox, toggle, nav item) the original doc demonstrated against. **Follow-up round, same day**: full channel audit added (`test/state-rules_test.js`) — introspects every state selector's declared CSS properties pairwise; confirmed hover's channel (`filter`) has zero overlap with anything (the specific thing asked); also surfaced the same outline collision on `.bx-selected`+focus already logged above, plus a harmless-by-design overlap among loading/readonly/disabled (all "dim+inert", mutually exclusive by usage, magnitudes differ .7/.55/.45 but never applied together). **Inspector panel reworked into a real detail panel**: `mountInspector` (`ui/inspector.js`) now measures `[data-name="topbar"]`/`[data-name="statusbar"]` in the mounted screen (when present — only `screens/math-trainer-dashboard.json` names both today) and docks the panel's `top`/`bottom` between them instead of spanning the full viewport over both; falls back to the old full-height behavior when a screen has neither (quiz/anatomy). Recomputed on window resize and after a root-level edit replaces the root element. **New `mountInspectorToggle`**: same reproducible-URL pattern as `ui/style-mode.js`'s `mountStyleToggle` (`?inspect=1`, off by default) — `.ins-toggle`/`.ins-toggle-inline` CSS mirrors `.style-toggle` exactly but opposite corner/side so the two never collide. `quiz.html`/`math-trainer.html`/`anatomy.html` switched from an unconditional `mountInspector(...)` call to `mountInspectorToggle(...)`, matching each page's existing style-toggle placement choice (inline for quiz/math-trainer, fixed-corner for anatomy). Verified live in the Browser pane on all 3 real pages (screenshots) + `test/inspector_test.js` gained 3 new blocks (docking-with-chrome, docking-without-chrome, toggle on/off + URL persistence) — full suite green, plus `quiz_test.js`/`math-trainer_test.js` regression (unaffected, both green). **Round 3, wiring `error`/`loading`/`read-only` into quiz.html** (math-trainer already had `loading`): found 3 genuine (non-synthetic) hooks in the existing quiz flow — `ui/quiz.js` gained `mountQuizFromUrl(root, url, reg)`, extracting quiz.html's fetch into a reusable, directly-testable helper: shows `loading` on the empty `answers` container for the real fetch gap, and on failure (bad URL/non-OK response, a path this page never handled before -- would previously just leave a permanently-empty screen) renders an actionable `bx-error` "Retry" button that re-invokes itself. `quiz.html` simplified to a single `await mountQuizFromUrl(el, url, reg)` call. Revealed answer rows (`enterRevealed` in `ui/quiz.js`) now get `setActionableReadonly` — their per-row click listener was already gone at that point (`exitAnswering` removes it), read-only just makes that honest instead of leaving them looking falsely still-interactive (pointer cursor, focusable). Verified live that this doesn't break "click-to-continue" (clicking a revealed row still advances early via bubbling to root's listener, even through `pointer-events:none` — confirmed with a real coordinate click in the Browser pane, not just a synthetic `.click()`). New tests: `test/quiz_test.js` gained 2 blocks for `mountQuizFromUrl` (happy-path loading, and a real-404 error/retry path) plus read-only assertions added to the existing reveal block; full suite green (quiz/math-trainer/gallery/node, no regressions). **`anatomy.html` explicitly NOT wired**: unlike quiz/math-trainer, it has no real interactive/async logic yet (script only does render+drawPaths+wireRelations+applyMotion+mountInspectorToggle — no click handlers on its table/pagination/search-field/segmented-control mockup pieces) — wiring error/loading/read-only there would be decorative/fabricated, not a real state, which breaks the precedent this round established. Needs real interactivity built first if these states are wanted there; flagged to the user rather than faked. **User chose to build that real interactivity** (round 4): `screens/anatomy.json`'s decorative, unnamed `component/data-table`/`component/pagination` usage replaced with a real, named `records-list` (3 static rows, one archived) + `pagination` (4 named buttons: page-prev/page-1/page-2/page-next) -- both parts stay in the registry/gallery unaffected (`component/data-table`/`component/pagination` invariants still pass, just no longer referenced from this one screen). New `ui/anatomy.js` (`mountRecordsPager(root, url)`, same page-mount bucket as `ui/quiz.js`): page 1 ships baked into the static JSON (so the existing `test/anatomy_test.js` screen-wide `diff(capture(root), resolve(doc))` invariant still holds exactly, zero conflict -- deliberately designed this way, verified live before writing any test); paging to page 2 (or back to 1 after leaving it) is a real `fetch('/content/anatomy/records.json')`, with real `loading` on the list, `read-only` on each page's one archived record, and `error`+"Retry" on a failed/404 fetch (url is a parameter specifically so tests can force the failure path, same pattern as `mountQuizFromUrl`). One real bug hit + fixed while testing: `mountRecordsPager` unconditionally looked up page 1's hardcoded `record-2` to mark it read-only, which threw `Cannot read properties of null` against a differently-shaped test fixture with no such node -- guarded with a null check (screens/anatomy.json's real page always has it, a test isolating just the error path legitimately doesn't). Verified: live in the Browser pane (screenshots, page 1 -> 2 -> back to 1 round-trip, real archived-row dimming) + `test/anatomy_test.js` gained 2 new blocks (pager round-trip incl. read-only assertions, isolated error/retry fixture) + full regression sweep green (anatomy/quiz/math-trainer/gallery test pages, node suite 66/66 on the 5 files unaffected by the still-open Windows registry.js issue). **Round 5: brand colour restored for mockup skin** -- user missed the "intensive" blue (`#2f5fd8`) from the original Claude Design import (approach D used it for every primary surface); the shipped implementation had instead reused the plain neutral `tint3` token for `atom/button.primary`, so mockup skin never got any real colour. Fixed the right way, not a global recolour: `tint3` is heavily overloaded (16 other part files use it for unrelated neutral fills -- scrollbar thumb, chart bars, badges, etc.) so recolouring it directly would have tinted all of those blue too. Instead added a genuine new `fill-tint` vocabulary token, `brand` (`ui/vocabulary.json`), used ONLY by `atom/button.primary` (`{"extends":"atom/button","box":"brand"}`, was `"tint3"`) -- C2-clean, one name for one concept, no accidental aliasing. `.bx-brand` in wireframe = identical `tint3` grey as before (zero visual change there, matches the user's own earlier choice to keep wireframe monochrome); mockup gets a real definition (`--brand:#2f5fd8`, `--brand-surf:#e4eafb`) styled the same way the original design's own primary button actually looked (light-blue surface + blue text/border, NOT a solid-blue-fill+white-text block, which was a common misreading avoided by re-checking the source CSS's `.btn.p` rule before implementing). Real bug caught before shipping: `.bx-brand` isn't a `bx-tint*` class, so it would have wrongly matched the recent bare/untinted-hover fix's selector and gotten a redundant hover background wash on top of its own fill -- added `:not(.bx-brand)` to that selector's exclusion list, with a regression test. Flows automatically to every existing `atom/button.primary` usage via the extends chain (quiz's Lock/Next buttons, math-trainer's primary-action) with zero screen-level edits needed. Verified: live screenshots (both skins, both screens, disabled-primary state too) + full regression sweep green (gallery invariant on `atom/button.primary` itself, quiz/math-trainer/state-rules test suites, node suite 66/66, full-registry resolve sweep confirming no other part broke from the new vocabulary token). **Round 6: inspector couldn't actually drive the states it was built to show, plus a real quiz styling audit surfaced a latent bug.** Inspector gaps found live: (1) a real click can never select a `pointer-events:none` box (disabled/loading/readonly) -- `e.target` never lands on it, same reason a real mouse click can't either; fixed with a reference-counted `.ins-inspecting` class on `<html>` (`setInspecting()`, survives partial teardown when multiple inspectors are mounted at once) plus a scoped CSS override (`.ins-inspecting .bx-disabled,...{pointer-events:auto}`). (2) no way to actually *set* selected/correct/wrong/loading/error/readonly from the panel -- added a dedicated "state classes (not box dials)" section (`buildStateToggles`/`populateStateToggles`/`setStateClass`, `ui/inspector.js`) with its own checkboxes, deliberately separate from the vocabulary-driven dial fields since these are plain CSS classes, not box dials (C2) and have no capture()/render() round-trip; loading/error/readonly reuse `ui/actions.js`'s existing setters so tabIndex stays correct. State classes are carried across a dial edit's element-replacement (`carryStateClasses`) since capture()/render() never see them and would otherwise silently drop them. (3) fixing (1) made a latent bug newly observable: `attachHandles` force-set `tabIndex=0` but `detachHandles` never restored the prior value, so selecting any inert box (tabIndex -1 by design) through the inspector permanently promoted it -- fixed with a `priorTabIndex` WeakMap in `ui/handles.js`. (4) `select()` never called `.focus()`, so handles' own focusin-triggered 50% reveal never fired via a real click-select (only via mouse-dwell) -- user-reported as "i miss focus" / "only quiz-body is draggable/resizable" (a big box's mouse-dwell reveal was masking the same missing-focus bug on smaller ones); fixed with `selected.focus({preventScroll:true})` right after `attachHandles`. Then, per explicit "check the disable/wrong/readonly styling in quiz too" audit request: `.bx-selected`'s `border-left:4px solid var(--ink)` had gone invisible on the now-pill-shaped answers (a pill has no flat edge for a same-color border-left to read against, imperceptible even at 8px, tested) -- user-confirmed fix (AskUserQuestion) changed it to `var(--ok)` (green, unused elsewhere, matches the original design doc's own "engaged" pick), which also exposed the *same* latent mockup-specificity bug `.bx-error` hit earlier (mockup's `.bx-solid{border-color:var(--ink)}` at higher specificity silently wins) -- always latent, invisible only while both resolved to the same `--ink`; fixed with a matching `:root[data-style="mockup"] .bx-solid.bx-selected` override. **Mid-audit interruption 1**: wrong overall result now auto-reveals the hint (same `visibility` mechanism the manual Hint!! button already used; a correct result doesn't auto-reveal, dedicated negative-case test added) and the title/hint get *text* colour (not the answer rows' own background tint) on reveal -- explicit "do not color title background" ask, enforced via `[data-name="prompt"].bx-correct/.bx-wrong{background:none;color:...}` beating the generic `.bx-correct`/`.bx-wrong` background rule per-property. **Mid-audit interruption 2**: "Lock answer in"/"Next" merged into one `btn-action` element (`screens/quiz.json`, `ui/quiz.js`) -- labeled "Check" while answering (enabled once multi-select has a listener), relabels to "Next" the instant `enterRevealed` runs (before next-ready enables it, so it's never left looking actionable with no listener attached -- the same "looks actionable, does nothing" bug this button already had once). `test/quiz_test.js` updated throughout (`btn-lock`/`btn-next` selectors -> `btn-action`, label assertions added). Verified: full regression sweep green after every step, including both interruptions -- `quiz.html`/`math-trainer.html`/`anatomy.html`/`index.html` gallery browser suites + node suite 66/66; `test/handles_test.js` and `test/inspector_test.js` gained dedicated blocks for the tabIndex-restore fix and the new state-toggle mechanism respectively. Not committed this round (standing "do not auto-commit for now" instruction). | | 2026-08-30 | 2026-08-31 |
| TODO-7 | `luna` skin — THW-Lernkarten style as 3rd `data-style` value | Medium | in-progress | **Round 1 (2026-09-09) shipped steps 1–8**: `docs/luna.reference.css` (THW style block, fonts stripped) · `ui/fonts/` 5 woff2 (THW embedded byte-identical files per weight for Source Sans + JetBrains, SHA256-verified → one file + weight range each; Saira 3 real cuts) · base `:root` gained skin knobs `--ok-surf --warn --warn-surf --wrong --wrong-surf --focus --r-rounded --display --shadow` with values reproducing the old hardcoded ones (wireframe/mockup pinned unchanged by test) · `.bx-rounded/.bx-correct/.bx-wrong/focus-visible` read those vars · `:root[data-style="luna"]` block + luna-only rules (solid focus, Saira on `.bx-brand/.bx-ok/.bx-warn` + prompt) · every mockup "realistic" rule is now an explicit `mockup, luna` selector list (NOT `:not(wireframe)`: fixture pages without `data-style` must stay wireframe) · `fill-tint` += `ok`,`warn` (+ CSS, + hover-exclusion) · `style-mode.js` 3-way cycle, label = next mode. THW's blue hover BORDER deliberately not ported (border-color = error's channel). **Found live + fixed**: committed `ui/quiz.js` tinted the hint panel on reveal but reset the hint text's classes on re-entry → question 2's panel stayed amber; `enterAnswering` now resets `hintPanelEl`, test asserts it, dead `[data-name="hint-text"]` colour rules removed. **Also found live (unrelated, fixed)**: `render()` wrote `data-box` unconditionally while `capture()` read it by truthiness → `box:""` (hint-text, legal zero-dial box) failed the gallery C6 invariant on `screens/quiz`; now presence-based both ways, regression in `test/render_test.js` (same bug class as the earlier `content:""` fix). Verified: `quiz.html?style=luna` screenshots (Saira title, blue CHECK, 8px, soft shadow, amber wrong / green correct), mockup screenshot unchanged, fonts `document.fonts.check` true, 2 new skin blocks in `test/state-rules_test.js`, quiz/state-rules suites green, node 66/66. **Round 2 (same day) step 9**: variants `atom/chip.active` (brand) · `chip.ok` · `chip.warn` (THW status/level pills are pill *labels* = chip, not the 14px count `badge` the spec first named) · `button.hint` (warn) · `button.know` (ok) · `text.plate` (brand, font:44, THW code-plate). Gallery invariant 71/71. **Windows `test/node/registry.js` fixed en route** (`fileURLToPath` instead of `.pathname`, plus `\`→`/` on glob results) — `parts_validate_test`/`answer_variant_test` run on Windows for the first time, node 76/76 (was 66/66 across only the 5 registry-free files). **Round 3 (same day) step 10, decision 4 = new `ui/thw.js`** (user pick over extending quiz.js): `screens/thw-card.json` (h:540, w:380, `gap:2++`; slots `level`/`prompt`/`body` re-rendered from parts) · `thw.html` · `content/thw/units.json` (8 cards, 2 categories, fantasy distractors) · ladder A→B→C→Lösung with points 25/15/10/5, wrong pick = `bx-wrong` + read-only options + disabled skip + auto-climb after 650ms, failed card re-queued, stats chips. Not ported: XP/ranks/combo/localStorage/category chips/FAQ. Fit measured per checklist #3 (question card at Stufe B/C = 480 exactly, code-card plate = 520 with spacers at base → 540, 20px cushion for cross-OS font metrics). Tests: `test/thw_test.js` 4 blocks green, `test/node/thw_test.js`, gallery invariant. **Step 11**: luna's focus override joined the channel audit (same-state allow-list, asserted `outline-*` only). All 11 spec steps shipped; TODO-7 stays open only for the follow-ups below. Possible follow-up: share `mountXFromUrl` between quiz.js/thw.js (accepted duplication for now). Source `D:\_project\202601_THW\thw_lernkarten.html` (external, 1MB, not copied). Spec: `docs/superpowers/specs/2026-09-09-luna-skin-design.md`. 3 C9 decisions taken: (1) 3rd skin `luna`, not a mockup replacement; (2) fonts as local woff2 under `ui/fonts/` (Saira Condensed, Source Sans 3, JetBrains Mono, all OFL, ~230KB); (3) `fill-tint` gains `ok` + `warn` (precedent `brand`) for THW's static green/amber buttons. Plan: new base vars (`--ok-surf --warn --warn-surf --wrong --focus --r-rounded --display --shadow`) so radius/wrong-hue/focus/display-font become skin-driven with wf/mockup visually unchanged; luna var block; 3-way toggle in `ui/style-mode.js`; proof = `quiz.html?style=luna`; then variants `chip.active button.hint button.know text.plate badge.ok badge.warn`; then a THW card screen (open decision 4: extend `ui/quiz.js` schema vs new controller). Known tradeoff: font-family change reflows hug boxes on toggle (mockup avoided this on purpose) — accepted, luna is a product skin. Out of scope: dark mode, disclosure part, XP/rank logic. | | 2026-09-09 | 2026-09-09 |
| TODO-8 | fayf_ui in fayf_processor — design-tool vendoring + capture→ui-kit bridge spike | Low | in-progress | **Shipped 2026-09-09 as fayf_processor TODO-189** (that repo's `.ai/todo.md` holds the detail; here only what touched fayf_ui): `just build` → committed `registry.json` + `test/node/registry_fresh_test.js` (a vendored snapshot needs the registry as a static file, no server); `.claude/launch.json` gained `fayf_processor-api` (port 8001) for cross-repo verification. Processor side (uncommitted there — that tree carries another session's work): vendored 23 files via its existing receipt script pinned to `28ae0a4` (needs this repo **pushed** for the raw URLs to resolve; `FAYF_UI_SRC` env reads from disk meanwhile), `frontend/mockups/fayf-skin.css` aliases our *mockup* skin vars to `design-tokens.css` (zero fayf_ui edits, CG-DS1 truth, dark mode free) and styles our `state` dial (`done/running/blocked/cancelled`) as their status pills, `frontend/mockups/fayf.html?screen=<slug>` renders processor-owned screen JSON, `?shell=1` hosts it in a ui-kit `Shell` via `Embed` — the bridge spike's answer: **no dial→token converter needed for design review**, Embed already does it. Their server needed `.json`/`.woff2` MIME (our ESM JSON import). 16/16 page checks, pytest green. Left open there: port more mockup pages / `screen_flows.md` sketches; converter only if a product-CSS use case ever appears. Original assessment: spec `2026-09-09-luna-skin-design.md` §C. Facts: processor frontend is vanilla/no-npm with its own `ui-kit` token vocab; CG-DS1-5 forbid raw px + non-box-shadow focus → fayf_ui CSS can't ship as product CSS, but `frontend/mockups/*.html` is CG-DS-exempt hand-written design-delta pages = exactly fayf_ui's job. Both systems on the 4px raster (`pad:2` = `inset2`), both JSON-spec driven (`capture()` / `UI.project()`). Recommended: option 1 (vendor `ui/`+`parts/` to `frontend/vendor/fayf_ui/` with `RECEIPT.json`, add a `processor` skin aliasing `--color-*` so CG-DS1 stays single truth, port one mockup page as screen JSON) then a 1-day spike on a dial→ui-kit-token converter. Not recommended: replacing ui-kit at runtime. Gate: processor CG7/CG9 + its communication-formats before editing there. Depends on TODO-7 step 8 (skin mechanism proven twice). | TODO-7 | 2026-09-09 | 2026-09-09 |
| TODO-9 | fayf_ui as THE frontend framework — C11 controllers (L9), shell screen, table/tree/form controllers, processor becomes a stub | High | in-progress | Direction set 2026-09-11 (inverts TODO-8's split): luna style (THW) + ui-kit's action handling/JSON config move INTO fayf_ui; fayf_processor keeps screen refs + machine JSON + pure handlers + API glue. Amendment C11 drafted (CONSTITUTION.md, CLAUDE.md ladder L9). Plan: `docs/superpowers/plans/2026-09-11-workspace-dashboard.md` — S1 machine.js · S2 shell.json · S3 table.js · S4 dashboard screen · S5 processor stub · S6 page-by-page (issues → list → browse → records/run → graph via Embed). Migration order per user: workspace + dashboard first. **S1a shipped 2026-09-11**: `ui/machine.js` (`init`/`step` pure, `validateMachine`/`validateEffect`, `mountMachine` — delegated `<name>.<event>` listeners, slot paint from `view(status)`, guard = element enabled iff a trigger of its name fires in the current state, effects `fetch/emit/timer` with injectable `io`), `vocabulary.json` `effect` list, node `test/node/machine_test.js` 16 (suite 100/100), browser `machine.html` + `test/machine_test.js` 26 checks (fetch ok/err, timer, emit, guards, C2 errors). Naming rule found live: effect triggers name a source (`timer.done`), never an element. **S1b shipped 2026-09-11**: `ui/quiz.js` re-based — `machines/quiz.json` (idle/loading/error/answering/revealed/next-ready/finished), pure `handlers` + `view`, mount = `mountMachine` only; `test/node/quiz_machine_test.js` 10 flow tests (suite 110/110); browser quiz/math-trainer/machine suites green unchanged. Controller grew for it: `send` effect (self, sync — a handler picks the transition by sending a named trigger, table stays static), `root` reserved trigger name (mounted element; surfaces never disabled), state-token patches (`actionable selected correct wrong readonly loading error disabled hidden`, absolute per paint), effects scoped to the state entry that started them (stale timer/fetch dropped), inert trigger = full no-op (handler not run), `morphChildren` keeps element identity across paints (same name+dials → update in place; focus/refs survive). **S2 shipped 2026-09-11** (two subagents: screen+parts / page+test): `screens/shell.json` (ws-head 44 · body row: side-panel w:200 + content fill + detail w:280 · status-bar 20; 28 names), `parts/cluster/nav-item` + `.active` (brand fill; cluster not atom — atoms have no children), `shell.html`, `test/shell_test.js` 70 checks (names, geometry, C10 fit, nav, model invariant, luna vars), registry 75 ids, node 110/110, gallery invariant green. Known: below ~560px width the fixed rails squeeze content (inherent); ws-head `between` with 3 children centres the crumbs; luna icon dots faint. **S3 shipped 2026-09-11** (two subagents: controller+node / fixture+browser): `ui/table.js` — pure `windowOf` · `sortRows` · `tableInit` · `tableHandlers(spec)` · `tableView(spec, t)`, composed as a sub-status (`status.data[spec.name]`) of the machine; rows = boxes `fixed h:28`, windowed with spacers (2000 rows → ≤ 20 painted), sort cycle none→asc→desc, row click emits `<name>.select`. machine.js: capture-phase listeners (scroll does not bubble), scroll payload carries scrollTop/clientHeight, and `measureScroll()` after each paint delivers a `<name>.scroll` once per clientHeight change (first paint, resize, content arrival) so windowing never waits for a user scroll. `table.html` + `test/table_test.js` 37 checks, `test/node/table_test.js` 19 → node 129/129; quiz/machine suites unchanged green. Contract lesson: `clamp` is a size token — cells use `clip`. **S4 shipped 2026-09-11** (two subagents): shell regions extracted to `component/ws-head` · `component/side-panel` · `component/shell-status` (shell.json + dashboard.json both extend them, 70 shell checks unchanged); `screens/dashboard.json` (glance = 4 `cluster/stat-tile`, `recent-runs` + `issues` scroll boxes h:240, detail); `machines/dashboard.json` (loading: 3 parallel fetches → `*.loaded` self-transitions → `flow.ready`; error/retry; nav-*.click → emit `nav.go`; btn-theme → emit `theme.toggle`); `ui/dashboard.js` (counts, two table slices via `tableInit/tableHandlers/tableView`, detail on select, `mountDashboard(root, reg, opts)`); fixtures `content/dashboard/{runs,pipelines,issues}.json`; `dashboard.html`; `test/dashboard_test.js` 77 checks; `test/node/dashboard_machine_test.js`; node 143/143. machine.js rule change: **self-transition = stay, no `enter` effects** (else `runs.loaded: loading` re-fires the fetches). **NEXT = S5** (processor stub) then S6 — see plan §Handover. **S6 issues shipped 2026-09-11**: first S6 page, master-detail issue triage. New sub-controller `ui/tree.js` (mirrors `ui/table.js`'s shape exactly — `treeInit`/`treeHandlers`/`treeView` on `status.data[spec.name] = {rows, open, sel}`; group header click toggles `open[group]` (undefined/true = expanded), item click selects + emits `<name>.select`; non-empty groups only, `groupOrder` order then any stray group alphabetically). `machines/issues.json` (loading → 1 fetch `/content/issues.json` → ready/error, 6 `detail-status-<v>.click` triggers, nav/theme/refresh same shape as dashboard's machine). `ui/issues.js`: `STATUSES` (6 lifecycle values, in-progress/open/ready/blocked/done/archived) · `MASTER` tree spec (groupKey status, labelKey title) · `makeHandlers(urls)` parametrized like table.js's spec (dynamic per-issue/per-status detail + status-write URLs) · selecting-on-click wraps `treeHandlers` the same way dashboard.js's `selecting()` wraps `tableHandlers`, adding a detail fetch effect alongside the emit · status-chip click sets status locally when `urls.status` is null (this repo's own demo) or POSTs + waits for `status.saved`/`status.failed` otherwise. `screens/issues.json` (dashboard.json's full-duplicate-structure pattern, not `extends`; `side-panel` children copied with `nav-issues` on `cluster/nav-item.active` — fixes the "always shows Dashboard active" gap for this screen only). Fixtures `content/issues.json` (9 issues, all 6 statuses, 2 each for 3 of them) + `content/issues/<id>.json` (created/page/url/body/context/console_errors). `issues.html`, `test/issues_test.js` (11 blocks, mirrors dashboard_test.js), `test/node/tree_test.js` (16 tests) + `test/node/issues_machine_test.js` (16 tests). Node suite 172/172 (was 143). Registry 81 ids incl. `screens/issues`. Browser-verified live (screenshots + console OK/FAIL sweep): all groups/counts/collapse/select/chip-switch/refresh/error-retry/nav/theme/skins/C2 green. **One spec deviation, flagged**: the spec's `detail-status` box string included a `wrap` token — not a real `ui/vocabulary.json` dial (no flex-wrap concept exists; C2 would throw "unknown value" at resolve/render, confirmed live). Used `scroll` (existing overflow token, same mechanism as table.js's row container) instead — keeps all 6 status chips reachable via horizontal scroll without overflowing the 280px detail panel (C10), no new primitive. **Known simplifications** (v1 scope, no new form-control primitive): no free-text issue filter, no inline body editing, no image/sketch embeds, no python-repr parsing of context fields (flat `String(value)` per key instead), nav rail still the existing 6-item dashboard set not the real page's 9 destinations (pre-existing S4/S5 simplification, not expanded), and the side-panel active-item fix (nav-issues highlighted, not nav-dashboard) applied only to `screens/issues.json` — `shell.json`/`dashboard.json` still show Dashboard active on every screen (pre-existing gap, left untouched here, noted for follow-up). **S6 list shipped 2026-09-11**: second S6 page, the Pipelines page — browse-only v1, no new sub-controller (two `ui/table.js` instances). `machines/list.json` (loading: 2 parallel fetches, reusing dashboard's own `content/dashboard/{pipelines,runs}.json` fixtures, no new fixture files). `ui/list.js`: `PIPELINES` (one-column picker, rows mapped from the raw string array to `{name}` objects) · `RUNS` (same 4 columns as dashboard's, but ALL runs, not just recent) · `initialData`/`handlers`/`view`/`mountList`. Clicking a pipeline row toggles `data.pipelineFilter` (same row again clears it) and re-derives the runs table; clicking a run row adds a NEW `run.open` emit (`{run_id}`) alongside the table's own `select` emit — no local selection kept, a run click is a real page nav in the ground truth (`run.html?run_id=`). Detail panel shows stats on the current (possibly filtered) view: total + per-status counts, sorted by count desc — no "select to inspect" concept on this screen. `screens/list.json` (dashboard.json's structure + issues.json's side-panel-override pattern, `nav-pipelines` active). `list.html`. `test/node/list_machine_test.js` (12 tests) + `test/list_test.js` (9 blocks) → node 184/184, registry 82 ids. Browser-verified live (console OK/FAIL sweep, all green). **Two real bugs found + fixed while building this** (full detail in the plan doc's S6 section): (1) `PIPELINES.name`/`RUNS.name` (`'pipelines'`/`'runs'`) collide with the natural raw-fetched-list key names the spec's own pseudocode used — a literal implementation silently loses the raw runs array the instant the table slice is written (breaks re-filtering to a second pipeline + the always-a-total status-text run count); fixed by keeping the raw list under a distinctly-named `data.allRuns` (pipelines needed no such fix, its table is never filtered — same "no second key for the same array" reasoning `ui/dashboard.js` already applies to ISSUES). (2) rebuilding the runs table via `tableInit()` on every pipeline-filter click resets `window.clientHeight` to 0, and `ui/machine.js`'s `measureScroll()` only re-delivers a `<name>.scroll` trigger when the container's REAL DOM height changes — a filter toggle repaints the same fixed-height box, so the table got stuck at ~9 rows (windowOf's "not yet measured" fallback) instead of a full ~20-row window until an unrelated resize; fixed by threading the previous `window.clientHeight` forward across a rebuild (only `scrollTop` resets). This second bug likely also affects `ui/dashboard.js`'s own refresh path (same `tableInit()`-on-every-load pattern, untested there — `dashboard_test.js`'s refresh block doesn't check row count) — out of this task's scope (dashboard.js untouched), flagged as a background task (`task_e416a594`) rather than fixed here. **Known simplifications** (v1 scope): no "start a run" form (dynamic record-id/value rows + JSON textarea) and no "quick: run Bundestag session" form (text field + JSON composition + id sanitization) — both need real text input, earmarked `ui/form.js` (next S6 page, records/run, needs it anyway); no free-text/status filter or export on the runs table (ground truth's `DataTable` had both); a pipeline-filter change resets the runs table's sort/scroll POSITION to defaults (accepted, `clientHeight` deliberately kept per the bug fix above). **browse shipped 2026-09-11**: third S6 page, files explorer -- new sub-controller `ui/filetree.js` (recursive, N-level, lazily-loaded directory tree, distinct from `ui/tree.js`'s flat grouped-list shape); read-only, JSON pretty-printed with NBSP-preserved indentation instead of a new CSS/vocabulary dial (closed 12-dial set); no edit, no structural JSON drill-down, no artefact grouping, no deep-link URL sync, single-pending-fetch limit, 500-line cap. Node 218/218; browser 60/60 (filetree + browse machine + browse_test.js). Full detail in the plan doc's 'browse shipped' note. **NEXT = records/run** (needs `ui/form.js` -- real text-input primitives, a bigger design decision, not to be improvised). **field primitive + start-a-run shipped 2026-09-11**: `field:"text"|"textarea"` node property (KISS, no C9 amendment -- the box model's 12-dial set stays closed) renders a real `<input>`/`<textarea>` instead of a div (`ui/render.js`); `ui/machine.js` gained `payload.value` on input/change events, a field target's `.value` synced by both the direct string-patch path AND `morph()`'s array-content path (a real bug caught live: morph() alone silently never updated `.value`, fixed same day). `ui/list.js` used it to finish the "start a run" v1 it deferred: one record-id field + Start button, shown when a pipeline is selected (reuses pipelineFilter, no new selection concept); `makeHandlers(urls)` factory added (`urls.startRun` null = local-optimistic demo, real POST for a consumer) -- same pattern `ui/issues.js` established. Node 221/221 (+3 for the start-run flow); browser list_test.js +1 block, all green, verified live (typed input, click Start, run.open emitted, field cleared). Still deferred: per-pipeline entry-schema (dynamic multi-row/JSON value fields), "quick: Bundestag session" convenience form. **NEXT = records/run** (records.js is 555 lines -- step navigation, JSON diff/edit, own large scope; run.js needs SSE live event streaming, a different engine gap entirely -- re-scope before diving in, don't assume it's a single small step like list/issues/browse were) then profile/query/annotate → graph via `Embed`. **S6 browse shipped 2026-09-11**: third S6 page, the files explorer — a genuinely lazy, recursive, N-level directory tree + a read-only detail pane. New sub-controller `ui/filetree.js` (a NEW file, `ui/tree.js` untouched — genuinely different shape: recursive nodes, not a flat grouped list). `t = status.data[spec.name] = { nodes, sel, pendingPath }`, `node = { name, path, kind, open, loading, error, children }` (`children === null` = unfetched, `[]` = fetched-empty); `filetreeHandlers` is pure and never decides to fetch — a dir click only toggles `open`, the fetch decision + single-in-flight-fetch guard (`pendingPath`) is the consumer's job (same wrap-the-click pattern as `ui/issues.js`'s `selectingMaster`/`ui/list.js`'s `selectingPipeline`); `setChildren`/`setLoading`/`setError` splice a new status at any depth via a small recursive `mapNode`, pure. `machines/browse.json` (loading: 1 fetch `/content/browse/mounts.json` → ready/error; `tree.click`/`level.loaded`/`level.failed`/`file.loaded`/`file.failed` self-transitions). `ui/browse.js`: `TREE` spec, `FIXTURE_URLS.level`/`.file`, `selectingTree(urls)` wraps `filetreeHandlers` (file click → fetch content; first-time dir expand → set `pendingPath` + fetch its level; a second pending dir click just toggles, no 2nd fetch). `detailBody` (exported, pure): JSON pretty-printed (raw string on parse failure) or passed through as-is, split on `\n`, each line's leading spaces replaced by the same count of U+00A0 (NBSP) so indentation survives under default `white-space:normal` — no CSS `white-space:pre`, no new vocabulary dial (closed 12-dial system, per the task's explicit constraint); capped at 500 lines + `'… truncated'`. `detail-title` shows the selected node's path (`tree.sel`, set synchronously on click) since the raw file response has no `path` field of its own. `screens/browse.json` (dashboard.json's structure; side-panel left as the untouched default — no `nav-browse` item exists in this repo's 6-item NAV set yet, so Dashboard stays marked active, a pre-existing gap issues.json/list.json each fixed locally for their OWN nav item but browse.json can't). Fixtures `content/browse/` — `mounts.json` (2 mounts) + a genuinely 3-level-deep tree under `runs/` and 2-level under `backend/` (exercises real recursion, not a flat demo). `browse.html`; `test/node/filetree_test.js` (15 tests) + `test/node/browse_machine_test.js` (19 tests) → node 218/218, registry 83 ids. Browser-verified live (screenshots + full `?test=browse.html` console sweep, 60/60 green): mount expand, 2-level-deep subdirectory expand, file select with visible NBSP-indented pretty JSON, collapse/re-expand without re-fetching, error/retry, nav/theme, C10 fit, luna skin, C2. Fixture path convention for the fayf_processor sibling: node `path` = `<mount>` or `<mount>/<sub/path...>`; `FIXTURE_URLS.level/file` append `.json` to that same string under `content/browse/level/`/`content/browse/file/` — real API call recovered by splitting on the FIRST `/` (`mount = path.split('/')[0]`, `rest = path.slice(mount.length+1)`). **Known simplifications** (v1 scope, all explicitly bounded by the task): no file editing/save (ground truth itself defers this too), no structural JSON table/drill-down (ground truth's bespoke `UI.project`/`UI.build`), no artefact-grouping heuristic (every entry shown as-is), no deep-link URL sync, single-pending-fetch limit (`pendingPath` guards one directory load at a time — documented, deliberate), no `nav-browse` side-panel item, 500-line detail content cap. Full detail: plan doc's S6 section. **S6 records shipped 2026-09-11**: fourth S6 page, "records" (first half of records/run — "run", the live SSE watch view, stays deferred, a different engine gap). No new sub-controller: reuses `ui/filetree.js` (unmodified) and `ui/browse.js`'s exported `detailBody` (imported directly, via a small `{format:'json', content}` adapter at the call site, since the artifact fixture is `{value, version}` not browse's raw `{format, content}`). Genuinely different from browse.html's usage though: every node here is known up front from ONE run-metadata fetch, so the tree is built fully-populated (`buildNodes()`, new, local to `ui/records.js`) rather than lazily — `filetreeInit`'s own mount-mapping helper is unsuited for this (it hardcodes `children: null`), so only its empty-shape contract is reused, `.nodes` supplied directly; `filetreeHandlers`/`filetreeView` consume the result unmodified either way. `machines/records.json` (loading: 1 fetch `run.json` → ready/error; `tree.click`/`detail.loaded`/`detail.failed` self-transitions in ready — a per-record fetch failure never demotes the whole screen to `error`, only sets `data.error`). `ui/records.js`: `TREE`, `FIXTURE_URLS.artifact(path)`, `FIXTURE_RUN_ID` (`'run-2026-09-11'`), `initialData(runId)` (takes a PARAMETER, unlike every prior page — a consumer must know the run before mounting), `selectingTree(urls)` (simpler than browse's — no lazy-load branch at all, a step click is always just the base toggle). `screens/records.json` (dashboard/issues.json's structure, `nav-records` active). Fixtures `content/records/run.json` (3 steps × 3 record_ids) + 4 of the 9 `content/records/artifact/<step>/<record>.json` combinations real (the other 5 deliberately missing, proving a clean `detail.failed` 404 path). `records.html`; `test/node/records_machine_test.js` (15 tests) + `test/records_test.js` (9 blocks) → node 236/236, registry 84 ids. Browser-verified live (screenshots + `?test=records.html` console sweep, all green after the last TestRunner marker). **Known simplifications** (all explicitly bounded by the task): no run-picker mode (`initialData(runId)` assumes a run already chosen), no named-channel-only step 3rd tree level (flattened to plain 2-level everywhere, a record under such a step won't resolve — accepted gap), no diff views, no in-place edit+save, no tags display, no "Re-run…"/graph-jump buttons. Full detail + the artifact-response-shape note for the fayf_processor sibling: plan doc's "records shipped" note. **NEXT = "run"** (SSE live event streaming — re-scope before starting, a different engine gap from records) then profile/query/annotate → graph via `Embed`. **S6 profile shipped 2026-09-11**: fifth S6 page, per-speaker credibility profiles across runs (ground truth `fayf_processor/frontend/profile.js`, 242 lines, behaviour only). No new sub-controller: one `ui/table.js` instance (`SPEAKERS`); the ground truth's run-checkbox multi-select and fenster `<select>` port as pure view/handler logic inline in `ui/profile.js` (same scale as `ui/issues.js`'s 6 status chips) — a dynamic-length run-picker uses ONE `run-picker.click` trigger + `p.path` prefix matching (`run-picker-<runId>`, same idiom `ui/table.js`/`ui/tree.js` use), the small fixed `WINDOWS` vocabulary (6 values) gets 6 real per-value triggers, same reasoning `ui/issues.js`'s `STATUSES` chips use. `machines/profile.json` (loading: 1 fetch `/content/profile/runs.json` → ready/error). `ui/profile.js`: `SPEAKERS` table spec, `WINDOWS`, `FIXTURE_URLS.profile(ids) = /content/profile/result-${ids.join('_')}.json`, `makeHandlers(urls)` factory (same urls-parametrized pattern as `ui/issues.js`/`ui/list.js`). `status.data.picked` = plain `{run_id: true|false}` map; `status.data.result` kept whole (raw `{sessions, profiles}`), the speakers table's rows are a derived mapped view over it (no second copy, same reasoning `ui/dashboard.js`/`ui/list.js` apply to issues/allRuns); a selected speaker's full record is looked back up by `speakerId`, not duplicated. Compute needs >= 2 picked runs — handler no-ops below that (belt-and-suspenders with the view's explicit `state:'disabled'` on `btn-compute`, same technique `ui/list.js`'s `starting` flag uses, since the machine's trigger-presence guard can't express a data condition). Content (wide) hosts run-picker + fenster chips + Compute + the 6-column speakers table; detail (fixed w:280, same as every other S6 page) hosts the selected speaker's findings — chosen since a 6-column table doesn't fit 280px and this page has only one selection concept. Ground truth's "0 gefunden must not look like nichts vergleichbar" honesty rule carried over: a 0-`paare_klassifiziert` speaker still gets a full table row + a "Keine klassifizierten Paare für diese Person." detail line, not a blank panel. `screens/profile.json` (dashboard.json's structure; side-panel left at its untouched default, Dashboard marked active — verified first against `parts/component/side-panel.json` and every page's own `NAV` constant that no `nav-profile` item exists in this repo's 6-item NAV set, same pre-existing gap already logged for shell/dashboard/browse). Fixtures `content/profile/runs.json` (8 runs: 4 done with distinct run_id/pipeline/started_at, 2 running, 2 failed) + `content/profile/result-run-2026-09-08_run-2026-09-03.json` (the two newest done runs, matching `FIXTURE_URLS.profile`'s id-join convention — 4 speakers, varied party/pairs/befunde, incl. `sp-lindner` at 0 pairs/empty befunde). `profile.html`; `test/node/profile_machine_test.js` (19 tests) + `test/profile_test.js` (10 blocks) → node 255/255 (was 236), registry 85 ids. Browser-verified live (`?test=profile.html` console sweep all green; also driven manually via JS in both wireframe and luna skins). **One real C10 fit bug found + fixed**: the task's own illustrative box strings (`run-picker` as `stack, gap:1, fill`, `fenster` as a bare `hug` row) both overflowed `content`'s measured width (unbounded-width chip/row text) — fixed with a bounded scrollable `run-picker` (`stack, gap:1, fixed, h:160, scroll, solid, rounded`, the same fixed-height+scroll idiom every other table container already uses) and `scroll` on `fenster` (identical fix class to `ui/issues.js`'s own `detail-status` chip row). **Also found**: an early draft's static label `run-picker-title` substring-collided with the dynamic `run-picker-<runId>` row names (harmless functionally — `ui/machine.js` routes by DOM ancestry, not name-prefix search — but broke a `qa('[data-name^="run-picker-"]')`-style test assertion); renamed to `picker-title` before shipping. Full detail: plan doc's "profile shipped" note.

**query shipped 2026-09-11**: sixth S6 page, a raw FQL query tool over the JSON artefacts (ground truth: `fayf_processor/frontend/query.js`, 262 lines — behaviour only, not copied; its own guided query-builder driven by `Api.querySchema()` is deferred IN THE GROUND TRUTH ITSELF, not a v1 cut made here). ONE machine state (`ready`, no loading/error split — mirrors the ground truth's own tolerance: a failed runs fetch just keeps the page scoped to "All runs", a query failure is `data.error`, not a page mode). No new sub-controller; one `ui/table.js` instance for the results grid, but UNLIKE every prior page its spec is genuinely dynamic per query (built fresh in the `query.loaded` handler, stored in `data.resultsSpec` alongside `data.results`, looked up at call time by `results.click`/`results.scroll` rather than a fixed top-level constant). `machines/query.json`; `ui/query.js` (`RUN_PICKER` single-select run chip — exclusive, not a toggle-set like profile.js's picks —, `EXAMPLES` (6 static chips), `FIXTURE_URLS` keyed by matching `data.q` against `EXAMPLES` for the fixture path (`/content/query/result-<index>.json`; a `run` gets tacked on as a harmless `?run=` query string the static server ignores; a free-typed/unmatched query falls back to `/content/query/result-none.json`, a path this repo does not ship, driving a clean 404 -> `query.failed`, not a crash), `makeHandlers(urls)` factory). `screens/query.json` — **deviation**: no `detail` side panel (unlike every other S6 page's 3-column body) — this page has no "select something, see detail elsewhere" concept the way issues/browse/profile/records do (the RESULT itself is the detail), and a dynamic-column results table needs the full content width more than a decorative 280px panel would earn its keep; everything lives in `content` (run-picker chips, example chips, the FQL field, Run, results). **Real bug found + fixed while building this** (would have shipped broken without the manual click-through the task explicitly asked for): `parts_validate_test.js`'s closed static-JSON key vocabulary (C2) does not allow `field` directly in a screen file (same reason `ui/list.js`'s `start-record-id` field is never in `screens/list.json` either) — fixed by making `screens/query.json`'s `fql-row` a plain static container and building the actual `field:'textarea'` node named `fql` dynamically inside `view()`'s content patch, same "field only ever comes from a view() patch" pattern list.js established. **Second real bug found only via a live `.click()` on the actual DOM element, not the automated suite alone**: the example-chip container was named `examples` (plural) in the screen JSON while the machine trigger is `example.click` (singular) — `ui/machine.js`'s click routing matches a trigger by an EXACT ancestor name, never a prefix, so neither the container (`examples`) nor an individual chip (`example-0`) matched `example.click` and every example click silently no-op'd (state looked fine at rest, so the automated suite's assertions on chip class-names alone would not by themselves have proven it — the fetch/results assertions did); renamed the container to `example` (exact match, mirrors `run-picker`'s own container-name-equals-trigger-name pattern). Fixtures: `content/query/runs.json` (5 runs) + `content/query/result-0.json` (EXAMPLES[0] "reactions by party", grouped) + `content/query/result-2.json` (EXAMPLES[2] "who heckled", flat, incl. a long `utterance` text column proving no special width handling is needed). `query.html`; `test/node/query_machine_test.js` (20 tests) + `test/query_test.js` (10 blocks) → node 275/275 (was 255), registry 86 ids. Browser-verified live (`?test=query.html` console sweep all green + manual real `.click()`-driven pass in both wireframe and luna skins, per the task's explicit ask for extra care after a past round's manual-only bug). **v1 simplifications** (noted, same discipline as every prior S6 page): no `?q=&run=` deep-link URL sync (same class of drop as browse.html's own); Run only fires on an explicit button click, not on textarea blur/commit (typing alone never runs); no per-column custom/wide-column widths (`ui/table.js`'s default equal `fill` width for every column); no client-side result filtering/export. **NEXT = annotate** then graph via `Embed`. | TODO-7, TODO-8 | 2026-09-11 | 2026-09-11 |

**annotate shipped 2026-09-11**: seventh S6 page, Session -> Rede -> paragraph/sentence drill-down + per-sentence coverage badge (ground truth: `fayf_processor/frontend/annotate.js`, 986 lines, EPIC-17 — behaviour only, not copied). **Scope is STORY-17.1 ONLY** — the ground truth's own header says this story is "read-only ... a progress hint only", written before 5 later stories (17.2-17.6) bolted record editing on top; the record/suggestion panel + "Übernehmen" take-over (17.2), the slot-form editor with closed-vocabulary Select fields (17.3 — needs a real form controller `ui/form.js` this repo does not have, a separate later C9 decision), D4 anchor mode, record delete (17.4), "Neuer Satz" fresh-suggestion re-run (17.5), gold export + findings (17.6) all stay on the old ui-kit page — none attempted even partially. No new sub-controller: the paragraph jump-list (`TREE`) is a flat array of rows built inline in `view()`, like `ui/query.js`'s chip rows. Two pure functions (`coverageSets`/`badgeFor`) ported LOGIC-only (not code, the ground truth is ui-kit/DOM) with the identical `"P<i>/S<j>"` id scheme. `machines/annotate.json`: single `ready` state (mirrors query.json's own no-loading/no-error-split reasoning). `ui/annotate.js`: `SESSION_PICKER`/`REDE_PICKER` single-select exclusive chip pickers (same toggle-clear idiom as `ui/query.js`'s `RUN_PICKER`); picking a session fetches its Reden (`urls.run`, new factory entry); picking a Rede fires segment+annotate fetches IN PARALLEL, no shared loading gate — the tree + sentence text render as soon as segment lands, badges upgrade (from a neutral `…` placeholder) once annotate lands too, ground truth parity ("text shows immediately, badges catch up"). `screens/annotate.json` (query.json's structure — no `detail` panel, no `nav-annotate` side-panel item, this repo's shared NAV set still the 6-item S4/S5 simplification) + a static plain-text `legend` line (no hover-tooltip mechanism in this box model, v1 simplification). Fixtures `content/annotate/` (4 sessions incl. one parent/child pair labeled `${parent_run_id} → ${pipeline} (Kind-Lauf)`, one Rede's segment — 3 paragraphs/6 sentences — + coverage exercising all 4 badge kinds: ✓ record, s skip, ⚠ L3-only, · offen). `annotate.html`; `test/node/annotate_machine_test.js` (20 tests) + `test/annotate_test.js` (9 blocks) → node 295/295 (was 275), registry 87 ids. Browser-verified live (`?test=annotate.html` console sweep all green, PLUS a manual `.click()` pass on real rendered chips/tree-rows/skin-toggle in both wireframe and luna). **One real bug found + fixed by that manual pass, not by the automated suite alone**: a sentence badge cell's box string was `'fixed, w:16, hug'` — two tokens for the same `size` dial in one string, a C8 violation ("uniqueness enforced per primitive at load"). This threw inside `render()` the first time a Rede was picked; `ui/machine.js`'s paint loop has no try/catch, so the exception fired right after the `tree` patch had already applied but before `sentences` could — tree counts rendered correctly while the whole text/badge panel silently froze on its placeholder. The automated suite caught it immediately as a "0 sentence rows" FAIL; reading the console error (`read_console_messages`) pinned the exact cause (`duplicate dial 'size': 'hug' vs 'fixed'`) fast. Fixed by dropping the redundant `hug`. **v1 simplifications**: no hover tooltip on a coverage badge (plain-text legend instead); no record/suggestion panel, slot-form editor, D4 anchor mode, record delete, fresh-suggestion re-run, or gold export (STORY-17.2-17.6, genuinely larger/separate features); `runs.failed`/`rede.failed` fail silently (no toast mechanism, same known gap every prior page's failed-fetch path carries); no `?run=&rede=` deep-link URL sync; no `nav-annotate` side-panel item. For the fayf_processor sibling: `urls.run(id)` -> `GET /api/runs/{id}` (`Api.run`); `urls.segment(runId, recordId)` -> `GET /api/step/{run}/segment/{recordId}` (`Api.artifact`); `urls.annotate(runId, recordId)` -> `POST /api/annotate/{run}/{record}/seed` (idempotent lazy-seed, treat as the read path, not a write to avoid); the machine's own hardcoded `enter` fetch needs `/api/runs?include_children=1` (child runs' own `record_ids` only exist at that granularity). Full detail: plan doc's "annotate shipped" note. **run shipped 2026-09-11**: sixth S6 page, closing out the records/run pair -- a live run-watch view (status badge, pause/resume/cancel, a live SSE event log, a steps table). The one real new engine capability this page needed: `ui/machine.js` gained a `stream` effect (`{stream:url, ok, err?}`) -- opens a real EventSource, every message dispatches `ok` with the parsed JSON body, stays open across many dispatches (unlike fetch/timer's fire-once) until the state that opened it ends, then is actively `.close()`d via a per-epoch `openStreams` map -- same "belongs to the state entry that started it" rule fetch/timer already follow, now with a real cleanup action instead of a silent drop. `opts.io.EventSource` is the injectable seam, mirroring `opts.io.fetch`/`setTimeout`. Shipped and tested BEFORE this page (2 new browser test blocks: open/multi-message/close-on-transition/error, via a fake EventSource class), so `ui/run.js` itself needed zero engine work -- status/pause/resume/cancel/steps table are all composed from existing primitives (`ui/table.js`, `init:{method:'POST'}` fetch effects same as `ui/list.js`'s start-a-run). `ui/run.js`: `STEPS` table spec, `FIXTURE_URLS` (`snapshot`/`events`/`action`), `initialData(runId)` (parametrized like `ui/records.js`), `makeHandlers(urls)` -- pause/resume/cancel POST with `init`, a `run.event` handler updates `data.status` from the message + appends to a 200-capped log, `run_finished` re-fetches the snapshot to refresh the steps table. Fixture demo fakes SSE via its OWN `opts.io.EventSource` replay-from-a-JSON-array class (in `run.html`'s bootstrap script, NOT `ui/machine.js` -- a test double at the same seam every other page's `opts.io.fetch` already uses), a real consumer points `urls.events` straight at a genuine `EventSource('/api/runs/{id}/events')`. Node 320/320 (was 295), browser `run_test.js` all green in a fresh tab (fake-stream replay visibly changes the status badge over real wall-clock time, pause/resume/cancel buttons track the current status correctly, a steps row click emits `records.open {run_id, step_id}`). Full detail: plan doc's "run shipped" note. **NEXT = graph via `Embed`** (the actual last S6 page now). | TODO-7, TODO-8 | 2026-09-11 | 2026-09-11 |
| TODO-5 | Implement too-much-space: V1 island default + V5 space-class, elastic `gap:min..max`, spacious variants | Medium | in-progress | Shipped so far (all green, `node --test test/node/*.js` + browser harness, incl. registry-wide invariant sweep + `quiz_test.js` interaction suite): `atom/text`/`.title`/`.hero` (`font:` numeric, validator-restricted, wired into `screens/quiz.json`'s `prompt`); gap growth-suffix parse/print (`gap:2` fixed · `gap:2+` · `gap:2++`, `grow_class` in vocabulary); pure `distributeGrowth(slots, excess)` in `ui/model.js` (L1, node-tested, kept as the documented/oracle model even though the shipped mechanism uses native CSS); **L2 render wiring is now done** — `ui/render.js`'s `gapSpacer()` interleaves real spacer elements (flex-grow=allowance, max-size=cap) instead of the CSS `gap` property whenever a box's gap dial is growth-suffixed, so native flexbox handles growth/capping/redistribution continuously with zero JS measurement or ResizeObserver; spacers carry no `bx` class so `capture()` already excludes them for free (round-trip invariant holds). Wired into `screens/quiz.json`'s `quiz-body` (`gap:2++`, its 4 static top-level children) — verified via `getComputedStyle` (spacers cap at 24px, zero overflow) and the full quiz interaction suite. Discovered + logged as checklist #18: a growth-suffixed gap on a container whose children are appended dynamically at runtime (e.g. `answersEl`) silently loses ALL spacing — so `answers`'s own internal gap stays fixed `gap:1`, not migrated. **Mechanism 2 (spacious variants) now shipped**: `parts/component/answer.json` extracted as the base answer-row part (was inline-only in `ui/quiz.js`), plus `component/answer.buzzer` (tinted 2-col grid cell, `w:136,h:52`, selector `w:18`) and `component/answer.spacious-list` (full-width sentence row, selector `w:18`) — both real `type.variant` parts (validator-checked), wired via the same `conditional`/`condition:['spacious']` mechanism TODO-4 shipped (`ui/vocabulary.json` gained the `spacious` condition token). Deviated from the spec's illustrative wiring (which put `conditional` on the plural `answers` *container*): the container's own box never varies, so `conditional` sits on each per-answer node instead (`ui/quiz.js`'s `answerNode()`), driven by a new optional per-question content field `"layout": "buzzer"\|"sentence"` (content shape is known at *content-authoring* time, not screen-authoring time, since one generic `screens/quiz.json` serves every lesson file) — `content/quiz/lesson1.json`'s 4 short numeric answers now set `"layout":"buzzer"`. Buzzer cells are sized for a 2-col grid but `answers` itself is a plain vertical `stack`, so `enterAnswering` pairs rows into row-wrapper elements two at a time when `layout==='buzzer'` (plain inline node, no new part) — `enterRevealed` switched from indexing `answersEl.children` to `querySelectorAll('[data-name^="answer-"]')` so it stays nesting-agnostic. `mountQuiz(root, quizData, reg)` gained a `reg` param (all 4 call sites updated: `quiz.html`, `math-trainer.html`, 3× `test/quiz_test.js`) since answer rows now resolve `extends` against the real registry. Verified: `test/node/answer_variant_test.js` (new, conditional resolve of the real parts under `env:['spacious']` vs `[]`) + full node suite (69/69) + browser `quiz_test.js`/`math-trainer_test.js` (all green, incl. the 2×2 grid DOM shape and click/reveal per-cell behavior) + gallery (`index.html`, 65/65 parts resolve). **Space-class auto-classification pipeline started**: `classify(excess)` (L1, pure, `ui/model.js`) maps a measured px excess to a condition token. Threshold `space_class.spacious_min: 96` registered in `ui/vocabulary.json` (C8), carries real evidence (`examples/too-much-space-sketch.html` panel B: ~185px leftover in a 460px-tall panel, comfortably above it — the exact case mechanism 2 targets).

**Now extended to the full `compact`/`cozy`/`spacious` 3-way split**: `classify()` always returns exactly one of the three (never empty), so a doc can condition on any of them explicitly. New `space_class.compact_max: 48` threshold added — honestly, this one does NOT have independent evidence yet; it's the simple midpoint of the 0..96 "not yet spacious" band, not a separately measured cutover (only `spacious_min` traces to a real data point). Revisit once more real screens are measured. `ui/vocabulary.json`'s `condition` enum gained `compact`/`cozy`. `ui/space-class.js`'s `wireSpaceClass()` simplified accordingly (the winning-token re-render is now unconditional, since `classify()` never returns `[]`); `container.dataset.spaceClass` now always shows one of the three names directly (no more `'base'` placeholder). `space-class.html`'s fixture doc gained a `cozy` conditional branch (`tint1`, between the unconditioned `compact` default and `spacious`'s `tint2`); `test/space-class_test.js` now exercises all three bands across two resize transitions. Verified: `test/node/model_test.js`'s expanded `classify()` cases (pure, node, 73/73 total) + the same manual live-invocation approach as before (excess/classify/resolve/render at three heights straddling both boundaries — compact/cozy/spacious all classified correctly) — the live `ResizeObserver` tick itself remains unverifiable in this session for the same pane-not-compositing reason logged above. `excess(container, island, axis)` (L2, `ui/render.js`) measures avail (container's content-box size) minus need (island's rendered natural size) — documented precondition: `container` must be a stable-sized (`fill`) box, not `hug`, or its own size would depend on the very island it's measuring, feeding back a moving target. New `ui/space-class.js` (`wireSpaceClass`, page-mount-layer glue like `ui/quiz.js`) wires it live: render the base (unconditioned) doc, measure, classify, re-resolve+re-render with the winning tokens if any, repeat on every `ResizeObserver` tick on `container`. New fixture `space-class.html` + `test/space-class_test.js` exercise it end-to-end (a synthetic conditional doc, not tied to any real screen — kept independent on purpose, see below). Verified: `test/node/model_test.js`'s new `classify()` cases (pure, node) + manual live invocation in-browser of the exact `excess()`/`classify()`/`resolve()`/`render()` call sequence at two different container heights straddling the threshold, both classifying correctly. NOT fully verified: the automatic `ResizeObserver`-driven reclassification itself — this session's preview browser pane wasn't compositing frames (confirmed independently: a bare `requestAnimationFrame` never fired either, a platform/tooling artifact of a hidden/non-displayed tab, not a code issue), so `test/space-class_test.js`'s live-resize assertions couldn't be exercised through to completion here. The test is left in as real regression coverage — run it via `just serve` + `/wrapper?test=space-class.html` in an actual visible browser (or from a session whose preview pane renders) to confirm. NOT yet wired into any real screen: `screens/quiz.json`'s `quiz-body` uses the pre-V1 "incumbent" pattern (`stack, fill, w:280, around, gap:2++` — itself `fill`, not a `hug` island wrapped in a `fill,mid,evenly` outer), which the design doc explicitly grandfathers ("stays legal, demoted to opt-in"), so `excess()`/`classify()` were built and tested against a clean, purpose-built V1-island fixture instead of retrofitting it. Retrofitting quiz-body hits a real, currently-unresolved structural question first: V1's margin model needs the island to be `hug`, but mechanism 1's elastic-gap growth needs its container to be `fill` (a `hug` box has zero excess of its own by construction — same reasoning as checklist #18) — those two sizing requirements can't both apply to the same box. The vocabulary's `clamped` size token (declared, unimplemented) looks like it's reserved for exactly this — grow like `fill` up to a cap, behave like `hug` beyond it — but deciding/implementing that is its own scoped piece of work, not something to improvise while wiring the classifier. **`quiz-body` rewired to match the sketch's "B · elastic gaps" panel**: flattened from 4 top-level children (`prompt`, `answers`, a `[hairline,controls]` sub-box, `hint-panel`) to 5 (`prompt`, `answers`, `hairline`, `controls`, `hint-panel`) — the sketch shows `hairline`↔`controls` as its own elastic seam, which the old grouped structure couldn't do (that pair was stuck at a fixed, non-elastic `gap:1` inside its own sub-box). Considered matching the sketch's 3 *independently-rated* gaps (`2++`/`2++`, `1+`, `2+`) exactly via nesting, but tested directly in-browser first: a nested `hug` (or the existing `clamped`, which only caps the width axis — `.bx-clamped{flex:1 1 auto;max-width:max-content}` in `ui/tokens.css`) sub-group has zero excess of its own to grow into (same reasoning as checklist #18), so only the outermost `fill` level can actually grow under the current mechanism; reproducing 3 independent rates would need either hand-computed per-group pixel caps (verified live, but goes stale as content length changes) or a new JS-measured clamp mechanism (reintroducing the per-frame measurement mechanism 1 was built to avoid). Given the choice, kept the single already-shipped uniform `gap:2++` across all 4 seams instead — visually close to the sketch, stays native-CSS-only, no new mechanism. Verified: full node suite (72/72), browser `quiz_test.js` (all green, incl. answer selection/reveal/grading unaffected by the flatter tree since lookups are all by `data-name`, nesting-agnostic), `math-trainer.html`'s in-place quiz mount (4 answers, 4 spacers, `controls`/`hint-panel` present), and `getBoundingClientRect` sweep of all 5 real children + 4 spacers (sequential, non-overlapping, each spacer at its 24px cap, zero overflow). Also dropped a stray `gap:1` that had appeared on the hairline leaf node from outside this session (no children, so it was a no-op) while restructuring that exact node. **`content/quiz/lesson1.json` now demonstrates the sentence layout for real**: added a 2nd question ("Which statement about prime numbers is true?", single mode, 3 full-sentence answers, `"layout":"sentence"`) — previously `component/answer.spacious-list` only existed as a gallery/spec artifact with no real content exercising it. `test/quiz_test.js` updated to match: the "pause gate then next" block's stale "lesson1 has only 1 question, so this was finish" assertion (now factually wrong now that lesson1 has 2) fixed to check the real outcome (advances to question 2), a new block verifies question 2 resolves `component/answer.spacious-list` (`h:52` in `data-box`, `w:18` selector) and drives it through to real "finish" for the first time with real content, and two other now-stale comments (the "Finding 3" synthetic-multi-question rationale, and the "single mode never driven through a real click" rationale) corrected rather than left misleading. Verified: full node suite (73/73, `quiz_content_test.js` unaffected since it only asserts generic schema, not question count) + browser `quiz_test.js` (all green) + manual click-through in both `quiz.html` and `math-trainer.html`'s in-place flow (sentences render full-width, un-truncated, in the spacious-list variant). **`clamped` given a real, defined CSS behavior**: previously declared in the vocabulary but never implemented meaningfully (`flex:1 1 auto;max-width:max-content` did nothing useful) and unused by any part/screen. Came up organically via `screens/quiz.json`'s `controls` needing to span `quiz-body`'s full width with "Hint" pinned left / Lock+Next pinned right — `between` distribute needs real extra width to push against, but `hug` (the only alternative to hardcoding a duplicate `w:N` matching the parent) always sizes to fit-content on both axes. Considered a new per-axis size syntax (`hug:V`/`fill:H` as two values for one dial, or two new `size-w`/`size-h` dials) — more general, but a bigger dial-model change (today one dial key holds one scalar value); decided against it in favor of reusing the already-reserved, already-unused `clamped` slot. Now: `.bx-clamped{flex:none;align-self:stretch}` — hug on the main axis (won't grow to eat leftover space, unlike `fill`), always stretches to fill the cross axis via `align-self` (robust regardless of the parent's own `align` token, unlike relying on a bare `fixed` with no `w` happening to inherit stretch by default). `controls` now reads `row, mid, between, clamped` — no more duplicated `w:380` to keep in sync with `quiz-body`'s own width. Verified: full node suite (73/73), `quiz_test.js`/gallery all green, live measurement confirms 0px offset from both edges and natural (unballooned) height. **`clamped` renamed to `clamp`** across `ui/vocabulary.json`, `ui/tokens.css`, every `parts/`/`screens/` usage, and its test assertions — user preference, no functional change. `docs/vocabulary.reference.html` deliberately left unchanged (it's a verbatim mirror of the external Claude Design source per `CLAUDE.md`, still says `clamped`) — this repo's vocabulary now locally diverges from that upstream naming until/unless renamed there too. `lesson1.json`'s Q1 now also uses `"layout":"sentence"` (was `"buzzer"`) so both questions render via `component/answer.spacious-list` — buzzer's part/mechanism/tests stay in place, just unused by current content. `answers`/`prompt` also switched `hug`→`clamp` so the whole `quiz-body` column (prompt, answers, hairline, controls, hint-panel) spans the full 380px width uniformly — previously only `controls` did. `quiz-body`'s `distribute` changed `around`→`packed` with a small explicit `gap:1` (was relying purely on `around`'s auto-spread, no explicit gap at all) to tighten the space between the 5 top-level components; `answers`'s own internal row gap bumped `gap:1`→`gap:2` in the other direction (a bit more breathing room between individual answers, per explicit request). **Grading logic fixed**: `ui/quiz.js`'s `enterRevealed` used to only color a correct-but-unselected answer green (informational reveal) with no indication the user's implicit "not this one" call was itself wrong; now `a.correct !== selected` (either direction of mismatch) colors red/`bx-wrong`, matching the existing red-for-selected-wrong-answer treatment — a correct answer the user missed reads the same as one they wrongly picked. Verified: full node suite (73/73), `quiz_test.js` all green including a new dedicated test for the missed-correct-answer coloring, gallery (65/65), and live measurement of the new tight/uniform-width layout in both `quiz.html` and `math-trainer.html`. Sketch: `examples/too-much-space-sketch.html`. Design: `docs/superpowers/specs/2026-08-29-too-much-space-design.md`. Checklist: `docs/checklist.md` #18. **2026-08-30: the "hug-vs-fill" tension resolved for `screens/quiz.json` specifically** (previously logged here as "a real, currently-unresolved structural question" -- not solved generally, just for this one screen): user asked to restore elastic gaps and add true vertical centering, both at once. Resolution: `quiz-body` switched from `fill` to `fixed, h:400` (a concrete, chosen budget, not a general mechanism) with `gap:2++` restored (was tightened to plain `gap:1` earlier, per a past explicit request logged above) -- gap spacers now grow up to their cap (measured live: ~23.7px each against a 24px cap, i.e. nearly maxed for lesson1's 4-answer question). Root gained `evenly` distribute alongside its existing `mid`, which centers the now-fixed-height `quiz-body` as a lone child -- measured live: exactly 261px margin top and bottom in a 922px viewport, both questions in lesson1 (4-answer and 3-answer content sizes both land on the same 400px card, just with slightly different leftover-after-cap absorbed as blank space at the bottom of the fixed box, `packed` distribute internally, unchanged). This is a **fixed-budget compromise, not the general fix**: `h:400` is a chosen constant tuned to this content, not a live measurement -- a screen with meaningfully taller/shorter content would need its own re-tuned `h:`. The general case (a `fill` island that both grows its own gaps AND reports genuine leftover-as-margin to an ancestor) still needs either hand-tuned per-screen caps (this) or new JS-measured clamp work, exactly as previously logged. Embedded context (`math-trainer.html`'s `work.appendChild` of `quiz-body` alone) inherits the fixed height + restored gaps but is NOT vertically centered there (no `evenly` on `work`) -- not requested, not touched, still top-anchored same as before. Verified: live in Browser pane (screenshots + `getBoundingClientRect` margin/spacer measurements) + full regression sweep green (quiz/math-trainer/gallery test pages, node suite 66/66). | TODO-4 (done) | 2026-08-29 | 2026-08-30 |
