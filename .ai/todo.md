# Project TODO List

Still-open follow-up items. Adapted from `fayf_smartskills/.ai/todo.md`'s
extended format.

**Schema:** `ID | Title | Priority | Status | Comment | Dependencies | Created | Updated`
- **Priority:** `Critical` / `High` / `Medium` / `Low`.
- **Status:** `open` / `in-progress` / `blocked` / `done` / `dropped`.
- **Comment:** why the item exists, current thinking.
- **Dependencies:** comma-separated IDs this item can't usefully start before;
  empty when there's no real gating relationship.
- **Created / Updated:** `YYYY-MM-DD`.

Closing an item sets `Status: done`, `Updated`, then moves the row to
`.ai/archive.md` (newest-first) in the same change.

| ID | Title | Priority | Status | Comment | Dependencies | Created | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-1 | Author line-art icons for Shop/Training/Lecture/Free Learning | Medium | open | Only `icon-dashboard` ported so far (Heroicons v2 outline, MIT, via `infopedia_processor/frontend/icon-sprite.js`). Remaining 4 nav ids fall back to `ui/icons.js`'s shared `icon-placeholder` glyph (dashed-circle outline). Hand-author own line-art per that repo's own precedent (its `D-R4-INLINE` category icons), add to `ui/icons.js`'s `ICONS` list + sprite -- each replaces the placeholder glyph automatically once listed. | | 2026-08-28 | 2026-08-28 |
| TODO-2 | Wire real pointer-proximity rollout for the nav rail (closed → expanded on approach) | Medium | open | `component/app-nav-rail` (closed, icon-only) and `component/app-nav-rail.expanded` (icon+text) exist as static variants. The dynamic "roll out on approach" behavior needs a new L3 relation measure in `ui/relation.js` (today only `scroll-fraction` exists) — analogous to `component/scrollbar`'s existing `relation` usage, so the variant would still only declare it via JSON once the measure exists. | | 2026-08-28 | 2026-08-28 |
| TODO-3 | Build remaining workspace screens: Shop, Training, Lecture, Free Learning | Medium | open | Step1 shipped only `screens/math-trainer-dashboard.json`. Nav rail already scaffolds all 5 sections (4 disabled). Each new screen composes from existing L0-L7 parts, same pattern as the dashboard. | | 2026-08-28 | 2026-08-28 |
| TODO-6 | Interaction state rules — pressed/loading/error/read-only added to the actionable-box layer | Medium | in-progress | Imported from Claude Design handoff "Element state style rules" (`State Rules Wireframes.dc.html`, project `2baedd3e-1686-4433-8561-fcc1b58a9eaf`) — verbatim copy `docs/state-rules.reference.html`. Doc offered 4 alternative visual languages for encoding interaction state across button/link/checkbox/toggle/nav-item roles (A: opacity ladder, B: 9 named colours, C: monochrome weight/elevation, D: hybrid one-channel-per-state); user picked **D** per C9 one-at-a-time ask, since it's closest to what fayf_ui already shipped (hover=brightness filter, focus=dashed outline ring, engaged/selected=solid outline — each already its own channel). Spec: `docs/superpowers/specs/2026-08-30-state-rules-design.md`. Scoped down from the doc's 6 component roles to fayf_ui's one existing role-agnostic mechanism (`ui/actions.js`'s "actionable box") — inventing button/checkbox/toggle/nav-item as real parts is separate, unscoped work. Shipped: `ui/tokens.css` gained `.bx-loading`(opacity:.7, inert), `.bx-error`(border-color:accent, stays actionable), `.bx-readonly`(opacity:.55, inert), and `.bx-actionable:active` (pressed, inset box-shadow); `ui/actions.js` gained `setActionableLoading`/`setActionableError`/`setActionableReadonly` mirroring the existing `setActionableDisabled` pairing with tab order (error does NOT drop tab order — doc's own "Retry stays actionable" rule). New fixture `state-rules.html` + `test/state-rules_test.js` (self-contained, same role as `inspector.html`/`space-class.html`) — all green, plus manual real-Tab-key check in the Browser pane (screenshot + `getComputedStyle`) confirming states render identically in both wireframe and mockup skins (corrected an initial wrong assumption in the spec draft that these needed a mockup-only gate — checked against `tokens.css` first, existing hover/focus/disabled/selected already skin-agnostic). **Real bug found + fixed during testing**: `.bx-error{outline:...}` (first draft) silently lost to `:focus-visible`'s own `outline` rule on a focused+errored box — `outline` is non-stacking and focus-visible has higher specificity. Moved error to `border-color` (a channel nothing else claims) + a specificity-matched mockup-skin duplicate rule; added a regression test that introspects the stylesheet rules directly (`declaredProps()` in `test/state-rules_test.js`) rather than relying on triggering `:focus-visible` — confirmed via `node -e`-style probing that headless `.focus()`/`{focusVisible:true}` don't reproduce the browser's real focus-visible heuristic in this environment, so that specific proof stayed manual (Tab key, Browser pane), consistent with this repo's existing precedent for interaction-driven behavior untestable headlessly (see TODO-5's space-class `ResizeObserver` note). **Logged, not fixed**: `.bx-selected` (engaged) has the identical outline-collision with focus — predates this doc, is shipped/tested quiz behavior, out of scope to retrofit today; a simultaneously focused+engaged box shows one ring or the other, not both. **Also found, spun off separately** (not part of this entry): `test/node/registry.js`'s `loadRegistry()` uses `new URL(...).pathname`, which returns a Windows-broken leading-slash drive path (`/D:/...`) — `globSync` silently returns 0 files, so `just ci` fails 2 of its files (`parts_validate_test.js`, `answer_variant_test.js`) on Windows with no real regression. Verified the other 5 node test files unaffected (66/66 pass, they carry their own fixture registries). Not yet done: wiring any of the 4 new states into a real screen's content (explicit non-goal in the spec — which screen/component needs pressed/loading/error/read-only first is an open follow-up), and the component-role split (primary/secondary/tertiary button, checkbox, toggle, nav item) the original doc demonstrated against. **Follow-up round, same day**: full channel audit added (`test/state-rules_test.js`) — introspects every state selector's declared CSS properties pairwise; confirmed hover's channel (`filter`) has zero overlap with anything (the specific thing asked); also surfaced the same outline collision on `.bx-selected`+focus already logged above, plus a harmless-by-design overlap among loading/readonly/disabled (all "dim+inert", mutually exclusive by usage, magnitudes differ .7/.55/.45 but never applied together). **Inspector panel reworked into a real detail panel**: `mountInspector` (`ui/inspector.js`) now measures `[data-name="topbar"]`/`[data-name="statusbar"]` in the mounted screen (when present — only `screens/math-trainer-dashboard.json` names both today) and docks the panel's `top`/`bottom` between them instead of spanning the full viewport over both; falls back to the old full-height behavior when a screen has neither (quiz/anatomy). Recomputed on window resize and after a root-level edit replaces the root element. **New `mountInspectorToggle`**: same reproducible-URL pattern as `ui/style-mode.js`'s `mountStyleToggle` (`?inspect=1`, off by default) — `.ins-toggle`/`.ins-toggle-inline` CSS mirrors `.style-toggle` exactly but opposite corner/side so the two never collide. `quiz.html`/`math-trainer.html`/`anatomy.html` switched from an unconditional `mountInspector(...)` call to `mountInspectorToggle(...)`, matching each page's existing style-toggle placement choice (inline for quiz/math-trainer, fixed-corner for anatomy). Verified live in the Browser pane on all 3 real pages (screenshots) + `test/inspector_test.js` gained 3 new blocks (docking-with-chrome, docking-without-chrome, toggle on/off + URL persistence) — full suite green, plus `quiz_test.js`/`math-trainer_test.js` regression (unaffected, both green). **Round 3, wiring `error`/`loading`/`read-only` into quiz.html** (math-trainer already had `loading`): found 3 genuine (non-synthetic) hooks in the existing quiz flow — `ui/quiz.js` gained `mountQuizFromUrl(root, url, reg)`, extracting quiz.html's fetch into a reusable, directly-testable helper: shows `loading` on the empty `answers` container for the real fetch gap, and on failure (bad URL/non-OK response, a path this page never handled before -- would previously just leave a permanently-empty screen) renders an actionable `bx-error` "Retry" button that re-invokes itself. `quiz.html` simplified to a single `await mountQuizFromUrl(el, url, reg)` call. Revealed answer rows (`enterRevealed` in `ui/quiz.js`) now get `setActionableReadonly` — their per-row click listener was already gone at that point (`exitAnswering` removes it), read-only just makes that honest instead of leaving them looking falsely still-interactive (pointer cursor, focusable). Verified live that this doesn't break "click-to-continue" (clicking a revealed row still advances early via bubbling to root's listener, even through `pointer-events:none` — confirmed with a real coordinate click in the Browser pane, not just a synthetic `.click()`). New tests: `test/quiz_test.js` gained 2 blocks for `mountQuizFromUrl` (happy-path loading, and a real-404 error/retry path) plus read-only assertions added to the existing reveal block; full suite green (quiz/math-trainer/gallery/node, no regressions). **`anatomy.html` explicitly NOT wired**: unlike quiz/math-trainer, it has no real interactive/async logic yet (script only does render+drawPaths+wireRelations+applyMotion+mountInspectorToggle — no click handlers on its table/pagination/search-field/segmented-control mockup pieces) — wiring error/loading/read-only there would be decorative/fabricated, not a real state, which breaks the precedent this round established. Needs real interactivity built first if these states are wanted there; flagged to the user rather than faked. **User chose to build that real interactivity** (round 4): `screens/anatomy.json`'s decorative, unnamed `component/data-table`/`component/pagination` usage replaced with a real, named `records-list` (3 static rows, one archived) + `pagination` (4 named buttons: page-prev/page-1/page-2/page-next) -- both parts stay in the registry/gallery unaffected (`component/data-table`/`component/pagination` invariants still pass, just no longer referenced from this one screen). New `ui/anatomy.js` (`mountRecordsPager(root, url)`, same page-mount bucket as `ui/quiz.js`): page 1 ships baked into the static JSON (so the existing `test/anatomy_test.js` screen-wide `diff(capture(root), resolve(doc))` invariant still holds exactly, zero conflict -- deliberately designed this way, verified live before writing any test); paging to page 2 (or back to 1 after leaving it) is a real `fetch('/content/anatomy/records.json')`, with real `loading` on the list, `read-only` on each page's one archived record, and `error`+"Retry" on a failed/404 fetch (url is a parameter specifically so tests can force the failure path, same pattern as `mountQuizFromUrl`). One real bug hit + fixed while testing: `mountRecordsPager` unconditionally looked up page 1's hardcoded `record-2` to mark it read-only, which threw `Cannot read properties of null` against a differently-shaped test fixture with no such node -- guarded with a null check (screens/anatomy.json's real page always has it, a test isolating just the error path legitimately doesn't). Verified: live in the Browser pane (screenshots, page 1 -> 2 -> back to 1 round-trip, real archived-row dimming) + `test/anatomy_test.js` gained 2 new blocks (pager round-trip incl. read-only assertions, isolated error/retry fixture) + full regression sweep green (anatomy/quiz/math-trainer/gallery test pages, node suite 66/66 on the 5 files unaffected by the still-open Windows registry.js issue). **Round 5: brand colour restored for mockup skin** -- user missed the "intensive" blue (`#2f5fd8`) from the original Claude Design import (approach D used it for every primary surface); the shipped implementation had instead reused the plain neutral `tint3` token for `atom/button.primary`, so mockup skin never got any real colour. Fixed the right way, not a global recolour: `tint3` is heavily overloaded (16 other part files use it for unrelated neutral fills -- scrollbar thumb, chart bars, badges, etc.) so recolouring it directly would have tinted all of those blue too. Instead added a genuine new `fill-tint` vocabulary token, `brand` (`ui/vocabulary.json`), used ONLY by `atom/button.primary` (`{"extends":"atom/button","box":"brand"}`, was `"tint3"`) -- C2-clean, one name for one concept, no accidental aliasing. `.bx-brand` in wireframe = identical `tint3` grey as before (zero visual change there, matches the user's own earlier choice to keep wireframe monochrome); mockup gets a real definition (`--brand:#2f5fd8`, `--brand-surf:#e4eafb`) styled the same way the original design's own primary button actually looked (light-blue surface + blue text/border, NOT a solid-blue-fill+white-text block, which was a common misreading avoided by re-checking the source CSS's `.btn.p` rule before implementing). Real bug caught before shipping: `.bx-brand` isn't a `bx-tint*` class, so it would have wrongly matched the recent bare/untinted-hover fix's selector and gotten a redundant hover background wash on top of its own fill -- added `:not(.bx-brand)` to that selector's exclusion list, with a regression test. Flows automatically to every existing `atom/button.primary` usage via the extends chain (quiz's Lock/Next buttons, math-trainer's primary-action) with zero screen-level edits needed. Verified: live screenshots (both skins, both screens, disabled-primary state too) + full regression sweep green (gallery invariant on `atom/button.primary` itself, quiz/math-trainer/state-rules test suites, node suite 66/66, full-registry resolve sweep confirming no other part broke from the new vocabulary token). **Round 6: inspector couldn't actually drive the states it was built to show, plus a real quiz styling audit surfaced a latent bug.** Inspector gaps found live: (1) a real click can never select a `pointer-events:none` box (disabled/loading/readonly) -- `e.target` never lands on it, same reason a real mouse click can't either; fixed with a reference-counted `.ins-inspecting` class on `<html>` (`setInspecting()`, survives partial teardown when multiple inspectors are mounted at once) plus a scoped CSS override (`.ins-inspecting .bx-disabled,...{pointer-events:auto}`). (2) no way to actually *set* selected/correct/wrong/loading/error/readonly from the panel -- added a dedicated "state classes (not box dials)" section (`buildStateToggles`/`populateStateToggles`/`setStateClass`, `ui/inspector.js`) with its own checkboxes, deliberately separate from the vocabulary-driven dial fields since these are plain CSS classes, not box dials (C2) and have no capture()/render() round-trip; loading/error/readonly reuse `ui/actions.js`'s existing setters so tabIndex stays correct. State classes are carried across a dial edit's element-replacement (`carryStateClasses`) since capture()/render() never see them and would otherwise silently drop them. (3) fixing (1) made a latent bug newly observable: `attachHandles` force-set `tabIndex=0` but `detachHandles` never restored the prior value, so selecting any inert box (tabIndex -1 by design) through the inspector permanently promoted it -- fixed with a `priorTabIndex` WeakMap in `ui/handles.js`. (4) `select()` never called `.focus()`, so handles' own focusin-triggered 50% reveal never fired via a real click-select (only via mouse-dwell) -- user-reported as "i miss focus" / "only quiz-body is draggable/resizable" (a big box's mouse-dwell reveal was masking the same missing-focus bug on smaller ones); fixed with `selected.focus({preventScroll:true})` right after `attachHandles`. Then, per explicit "check the disable/wrong/readonly styling in quiz too" audit request: `.bx-selected`'s `border-left:4px solid var(--ink)` had gone invisible on the now-pill-shaped answers (a pill has no flat edge for a same-color border-left to read against, imperceptible even at 8px, tested) -- user-confirmed fix (AskUserQuestion) changed it to `var(--ok)` (green, unused elsewhere, matches the original design doc's own "engaged" pick), which also exposed the *same* latent mockup-specificity bug `.bx-error` hit earlier (mockup's `.bx-solid{border-color:var(--ink)}` at higher specificity silently wins) -- always latent, invisible only while both resolved to the same `--ink`; fixed with a matching `:root[data-style="mockup"] .bx-solid.bx-selected` override. **Mid-audit interruption 1**: wrong overall result now auto-reveals the hint (same `visibility` mechanism the manual Hint!! button already used; a correct result doesn't auto-reveal, dedicated negative-case test added) and the title/hint get *text* colour (not the answer rows' own background tint) on reveal -- explicit "do not color title background" ask, enforced via `[data-name="prompt"].bx-correct/.bx-wrong{background:none;color:...}` beating the generic `.bx-correct`/`.bx-wrong` background rule per-property. **Mid-audit interruption 2**: "Lock answer in"/"Next" merged into one `btn-action` element (`screens/quiz.json`, `ui/quiz.js`) -- labeled "Check" while answering (enabled once multi-select has a listener), relabels to "Next" the instant `enterRevealed` runs (before next-ready enables it, so it's never left looking actionable with no listener attached -- the same "looks actionable, does nothing" bug this button already had once). `test/quiz_test.js` updated throughout (`btn-lock`/`btn-next` selectors -> `btn-action`, label assertions added). Verified: full regression sweep green after every step, including both interruptions -- `quiz.html`/`math-trainer.html`/`anatomy.html`/`index.html` gallery browser suites + node suite 66/66; `test/handles_test.js` and `test/inspector_test.js` gained dedicated blocks for the tabIndex-restore fix and the new state-toggle mechanism respectively. Not committed this round (standing "do not auto-commit for now" instruction). | | 2026-08-30 | 2026-08-31 |
| TODO-5 | Implement too-much-space: V1 island default + V5 space-class, elastic `gap:min..max`, spacious variants | Medium | in-progress | Shipped so far (all green, `node --test test/node/*.js` + browser harness, incl. registry-wide invariant sweep + `quiz_test.js` interaction suite): `atom/text`/`.title`/`.hero` (`font:` numeric, validator-restricted, wired into `screens/quiz.json`'s `prompt`); gap growth-suffix parse/print (`gap:2` fixed · `gap:2+` · `gap:2++`, `grow_class` in vocabulary); pure `distributeGrowth(slots, excess)` in `ui/model.js` (L1, node-tested, kept as the documented/oracle model even though the shipped mechanism uses native CSS); **L2 render wiring is now done** — `ui/render.js`'s `gapSpacer()` interleaves real spacer elements (flex-grow=allowance, max-size=cap) instead of the CSS `gap` property whenever a box's gap dial is growth-suffixed, so native flexbox handles growth/capping/redistribution continuously with zero JS measurement or ResizeObserver; spacers carry no `bx` class so `capture()` already excludes them for free (round-trip invariant holds). Wired into `screens/quiz.json`'s `quiz-body` (`gap:2++`, its 4 static top-level children) — verified via `getComputedStyle` (spacers cap at 24px, zero overflow) and the full quiz interaction suite. Discovered + logged as checklist #18: a growth-suffixed gap on a container whose children are appended dynamically at runtime (e.g. `answersEl`) silently loses ALL spacing — so `answers`'s own internal gap stays fixed `gap:1`, not migrated. **Mechanism 2 (spacious variants) now shipped**: `parts/component/answer.json` extracted as the base answer-row part (was inline-only in `ui/quiz.js`), plus `component/answer.buzzer` (tinted 2-col grid cell, `w:136,h:52`, selector `w:18`) and `component/answer.spacious-list` (full-width sentence row, selector `w:18`) — both real `type.variant` parts (validator-checked), wired via the same `conditional`/`condition:['spacious']` mechanism TODO-4 shipped (`ui/vocabulary.json` gained the `spacious` condition token). Deviated from the spec's illustrative wiring (which put `conditional` on the plural `answers` *container*): the container's own box never varies, so `conditional` sits on each per-answer node instead (`ui/quiz.js`'s `answerNode()`), driven by a new optional per-question content field `"layout": "buzzer"\|"sentence"` (content shape is known at *content-authoring* time, not screen-authoring time, since one generic `screens/quiz.json` serves every lesson file) — `content/quiz/lesson1.json`'s 4 short numeric answers now set `"layout":"buzzer"`. Buzzer cells are sized for a 2-col grid but `answers` itself is a plain vertical `stack`, so `enterAnswering` pairs rows into row-wrapper elements two at a time when `layout==='buzzer'` (plain inline node, no new part) — `enterRevealed` switched from indexing `answersEl.children` to `querySelectorAll('[data-name^="answer-"]')` so it stays nesting-agnostic. `mountQuiz(root, quizData, reg)` gained a `reg` param (all 4 call sites updated: `quiz.html`, `math-trainer.html`, 3× `test/quiz_test.js`) since answer rows now resolve `extends` against the real registry. Verified: `test/node/answer_variant_test.js` (new, conditional resolve of the real parts under `env:['spacious']` vs `[]`) + full node suite (69/69) + browser `quiz_test.js`/`math-trainer_test.js` (all green, incl. the 2×2 grid DOM shape and click/reveal per-cell behavior) + gallery (`index.html`, 65/65 parts resolve). **Space-class auto-classification pipeline started**: `classify(excess)` (L1, pure, `ui/model.js`) maps a measured px excess to a condition token. Threshold `space_class.spacious_min: 96` registered in `ui/vocabulary.json` (C8), carries real evidence (`examples/too-much-space-sketch.html` panel B: ~185px leftover in a 460px-tall panel, comfortably above it — the exact case mechanism 2 targets).

**Now extended to the full `compact`/`cozy`/`spacious` 3-way split**: `classify()` always returns exactly one of the three (never empty), so a doc can condition on any of them explicitly. New `space_class.compact_max: 48` threshold added — honestly, this one does NOT have independent evidence yet; it's the simple midpoint of the 0..96 "not yet spacious" band, not a separately measured cutover (only `spacious_min` traces to a real data point). Revisit once more real screens are measured. `ui/vocabulary.json`'s `condition` enum gained `compact`/`cozy`. `ui/space-class.js`'s `wireSpaceClass()` simplified accordingly (the winning-token re-render is now unconditional, since `classify()` never returns `[]`); `container.dataset.spaceClass` now always shows one of the three names directly (no more `'base'` placeholder). `space-class.html`'s fixture doc gained a `cozy` conditional branch (`tint1`, between the unconditioned `compact` default and `spacious`'s `tint2`); `test/space-class_test.js` now exercises all three bands across two resize transitions. Verified: `test/node/model_test.js`'s expanded `classify()` cases (pure, node, 73/73 total) + the same manual live-invocation approach as before (excess/classify/resolve/render at three heights straddling both boundaries — compact/cozy/spacious all classified correctly) — the live `ResizeObserver` tick itself remains unverifiable in this session for the same pane-not-compositing reason logged above. `excess(container, island, axis)` (L2, `ui/render.js`) measures avail (container's content-box size) minus need (island's rendered natural size) — documented precondition: `container` must be a stable-sized (`fill`) box, not `hug`, or its own size would depend on the very island it's measuring, feeding back a moving target. New `ui/space-class.js` (`wireSpaceClass`, page-mount-layer glue like `ui/quiz.js`) wires it live: render the base (unconditioned) doc, measure, classify, re-resolve+re-render with the winning tokens if any, repeat on every `ResizeObserver` tick on `container`. New fixture `space-class.html` + `test/space-class_test.js` exercise it end-to-end (a synthetic conditional doc, not tied to any real screen — kept independent on purpose, see below). Verified: `test/node/model_test.js`'s new `classify()` cases (pure, node) + manual live invocation in-browser of the exact `excess()`/`classify()`/`resolve()`/`render()` call sequence at two different container heights straddling the threshold, both classifying correctly. NOT fully verified: the automatic `ResizeObserver`-driven reclassification itself — this session's preview browser pane wasn't compositing frames (confirmed independently: a bare `requestAnimationFrame` never fired either, a platform/tooling artifact of a hidden/non-displayed tab, not a code issue), so `test/space-class_test.js`'s live-resize assertions couldn't be exercised through to completion here. The test is left in as real regression coverage — run it via `just serve` + `/wrapper?test=space-class.html` in an actual visible browser (or from a session whose preview pane renders) to confirm. NOT yet wired into any real screen: `screens/quiz.json`'s `quiz-body` uses the pre-V1 "incumbent" pattern (`stack, fill, w:280, around, gap:2++` — itself `fill`, not a `hug` island wrapped in a `fill,mid,evenly` outer), which the design doc explicitly grandfathers ("stays legal, demoted to opt-in"), so `excess()`/`classify()` were built and tested against a clean, purpose-built V1-island fixture instead of retrofitting it. Retrofitting quiz-body hits a real, currently-unresolved structural question first: V1's margin model needs the island to be `hug`, but mechanism 1's elastic-gap growth needs its container to be `fill` (a `hug` box has zero excess of its own by construction — same reasoning as checklist #18) — those two sizing requirements can't both apply to the same box. The vocabulary's `clamped` size token (declared, unimplemented) looks like it's reserved for exactly this — grow like `fill` up to a cap, behave like `hug` beyond it — but deciding/implementing that is its own scoped piece of work, not something to improvise while wiring the classifier. **`quiz-body` rewired to match the sketch's "B · elastic gaps" panel**: flattened from 4 top-level children (`prompt`, `answers`, a `[hairline,controls]` sub-box, `hint-panel`) to 5 (`prompt`, `answers`, `hairline`, `controls`, `hint-panel`) — the sketch shows `hairline`↔`controls` as its own elastic seam, which the old grouped structure couldn't do (that pair was stuck at a fixed, non-elastic `gap:1` inside its own sub-box). Considered matching the sketch's 3 *independently-rated* gaps (`2++`/`2++`, `1+`, `2+`) exactly via nesting, but tested directly in-browser first: a nested `hug` (or the existing `clamped`, which only caps the width axis — `.bx-clamped{flex:1 1 auto;max-width:max-content}` in `ui/tokens.css`) sub-group has zero excess of its own to grow into (same reasoning as checklist #18), so only the outermost `fill` level can actually grow under the current mechanism; reproducing 3 independent rates would need either hand-computed per-group pixel caps (verified live, but goes stale as content length changes) or a new JS-measured clamp mechanism (reintroducing the per-frame measurement mechanism 1 was built to avoid). Given the choice, kept the single already-shipped uniform `gap:2++` across all 4 seams instead — visually close to the sketch, stays native-CSS-only, no new mechanism. Verified: full node suite (72/72), browser `quiz_test.js` (all green, incl. answer selection/reveal/grading unaffected by the flatter tree since lookups are all by `data-name`, nesting-agnostic), `math-trainer.html`'s in-place quiz mount (4 answers, 4 spacers, `controls`/`hint-panel` present), and `getBoundingClientRect` sweep of all 5 real children + 4 spacers (sequential, non-overlapping, each spacer at its 24px cap, zero overflow). Also dropped a stray `gap:1` that had appeared on the hairline leaf node from outside this session (no children, so it was a no-op) while restructuring that exact node. **`content/quiz/lesson1.json` now demonstrates the sentence layout for real**: added a 2nd question ("Which statement about prime numbers is true?", single mode, 3 full-sentence answers, `"layout":"sentence"`) — previously `component/answer.spacious-list` only existed as a gallery/spec artifact with no real content exercising it. `test/quiz_test.js` updated to match: the "pause gate then next" block's stale "lesson1 has only 1 question, so this was finish" assertion (now factually wrong now that lesson1 has 2) fixed to check the real outcome (advances to question 2), a new block verifies question 2 resolves `component/answer.spacious-list` (`h:52` in `data-box`, `w:18` selector) and drives it through to real "finish" for the first time with real content, and two other now-stale comments (the "Finding 3" synthetic-multi-question rationale, and the "single mode never driven through a real click" rationale) corrected rather than left misleading. Verified: full node suite (73/73, `quiz_content_test.js` unaffected since it only asserts generic schema, not question count) + browser `quiz_test.js` (all green) + manual click-through in both `quiz.html` and `math-trainer.html`'s in-place flow (sentences render full-width, un-truncated, in the spacious-list variant). **`clamped` given a real, defined CSS behavior**: previously declared in the vocabulary but never implemented meaningfully (`flex:1 1 auto;max-width:max-content` did nothing useful) and unused by any part/screen. Came up organically via `screens/quiz.json`'s `controls` needing to span `quiz-body`'s full width with "Hint" pinned left / Lock+Next pinned right — `between` distribute needs real extra width to push against, but `hug` (the only alternative to hardcoding a duplicate `w:N` matching the parent) always sizes to fit-content on both axes. Considered a new per-axis size syntax (`hug:V`/`fill:H` as two values for one dial, or two new `size-w`/`size-h` dials) — more general, but a bigger dial-model change (today one dial key holds one scalar value); decided against it in favor of reusing the already-reserved, already-unused `clamped` slot. Now: `.bx-clamped{flex:none;align-self:stretch}` — hug on the main axis (won't grow to eat leftover space, unlike `fill`), always stretches to fill the cross axis via `align-self` (robust regardless of the parent's own `align` token, unlike relying on a bare `fixed` with no `w` happening to inherit stretch by default). `controls` now reads `row, mid, between, clamped` — no more duplicated `w:380` to keep in sync with `quiz-body`'s own width. Verified: full node suite (73/73), `quiz_test.js`/gallery all green, live measurement confirms 0px offset from both edges and natural (unballooned) height. **`clamped` renamed to `clamp`** across `ui/vocabulary.json`, `ui/tokens.css`, every `parts/`/`screens/` usage, and its test assertions — user preference, no functional change. `docs/vocabulary.reference.html` deliberately left unchanged (it's a verbatim mirror of the external Claude Design source per `CLAUDE.md`, still says `clamped`) — this repo's vocabulary now locally diverges from that upstream naming until/unless renamed there too. `lesson1.json`'s Q1 now also uses `"layout":"sentence"` (was `"buzzer"`) so both questions render via `component/answer.spacious-list` — buzzer's part/mechanism/tests stay in place, just unused by current content. `answers`/`prompt` also switched `hug`→`clamp` so the whole `quiz-body` column (prompt, answers, hairline, controls, hint-panel) spans the full 380px width uniformly — previously only `controls` did. `quiz-body`'s `distribute` changed `around`→`packed` with a small explicit `gap:1` (was relying purely on `around`'s auto-spread, no explicit gap at all) to tighten the space between the 5 top-level components; `answers`'s own internal row gap bumped `gap:1`→`gap:2` in the other direction (a bit more breathing room between individual answers, per explicit request). **Grading logic fixed**: `ui/quiz.js`'s `enterRevealed` used to only color a correct-but-unselected answer green (informational reveal) with no indication the user's implicit "not this one" call was itself wrong; now `a.correct !== selected` (either direction of mismatch) colors red/`bx-wrong`, matching the existing red-for-selected-wrong-answer treatment — a correct answer the user missed reads the same as one they wrongly picked. Verified: full node suite (73/73), `quiz_test.js` all green including a new dedicated test for the missed-correct-answer coloring, gallery (65/65), and live measurement of the new tight/uniform-width layout in both `quiz.html` and `math-trainer.html`. Sketch: `examples/too-much-space-sketch.html`. Design: `docs/superpowers/specs/2026-08-29-too-much-space-design.md`. Checklist: `docs/checklist.md` #18. **2026-08-30: the "hug-vs-fill" tension resolved for `screens/quiz.json` specifically** (previously logged here as "a real, currently-unresolved structural question" -- not solved generally, just for this one screen): user asked to restore elastic gaps and add true vertical centering, both at once. Resolution: `quiz-body` switched from `fill` to `fixed, h:400` (a concrete, chosen budget, not a general mechanism) with `gap:2++` restored (was tightened to plain `gap:1` earlier, per a past explicit request logged above) -- gap spacers now grow up to their cap (measured live: ~23.7px each against a 24px cap, i.e. nearly maxed for lesson1's 4-answer question). Root gained `evenly` distribute alongside its existing `mid`, which centers the now-fixed-height `quiz-body` as a lone child -- measured live: exactly 261px margin top and bottom in a 922px viewport, both questions in lesson1 (4-answer and 3-answer content sizes both land on the same 400px card, just with slightly different leftover-after-cap absorbed as blank space at the bottom of the fixed box, `packed` distribute internally, unchanged). This is a **fixed-budget compromise, not the general fix**: `h:400` is a chosen constant tuned to this content, not a live measurement -- a screen with meaningfully taller/shorter content would need its own re-tuned `h:`. The general case (a `fill` island that both grows its own gaps AND reports genuine leftover-as-margin to an ancestor) still needs either hand-tuned per-screen caps (this) or new JS-measured clamp work, exactly as previously logged. Embedded context (`math-trainer.html`'s `work.appendChild` of `quiz-body` alone) inherits the fixed height + restored gaps but is NOT vertically centered there (no `evenly` on `work`) -- not requested, not touched, still top-anchored same as before. Verified: live in Browser pane (screenshots + `getBoundingClientRect` margin/spacer measurements) + full regression sweep green (quiz/math-trainer/gallery test pages, node suite 66/66). | TODO-4 (done) | 2026-08-29 | 2026-08-30 |
